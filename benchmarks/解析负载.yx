引「../src/言序HTTP.yx」为 HTTP；

引「标准:字节」为 字节；

定 空字节：字节串 为 字节.从文字（「」）；

令 请求次：数 为 0；

当 （请求次 小于 100） 则
    定 请求 为 HTTP.解析请求（「GET /items?page=2 HTTP/1.1\nHost: localhost\nCookie: theme=dark」，空字节）；
    若 （请求.路径 不等于 「/items」） 则
        抛 「普通请求解析负载结果错误」；
    终
    置 请求次 为 （请求次 加 1）；
终

令 分块次：数 为 0；

当 （分块次 小于 25） 则
    定 结果 为 HTTP.解析分块正文（字节.从文字（「4\nWiki\n5\npedia\n0\n\n」））；
    若 （字节.转文字（结果.正文） 不等于 「Wikipedia」） 则
        抛 「分块正文解析负载结果错误」；
    终
    置 分块次 为 （分块次 加 1）；
终

言 「HTTP 解析负载通过」；
