引「../src/言序HTTP.yx」为 HTTP；

引「标准:套接字」为 套接字；

引「标准:环境」为 环境；

引「标准:测试」为 测试；

引「标准:字节」为 字节；

定 参数列：列<文> 为 环境.参数（）；

测试.相等（长度（参数列），1）；

定 监听器：套接字 为 套接字.TCP监听（参数列【0】）；

定 已接受：典 为 套接字.接受（监听器，5000）；

定 连接：套接字 为 已接受【「套接字」】；

定 会话 为 HTTP.连接会话为（连接，HTTP.限制（）.设读取（5000，8192，65536，1）.设会话（8））；

定 首项 为 会话.读取请求（）；

测试.相等（首项.方法，「POST」）；

测试.相等（首项.路径，「/one」）；

测试.相等（首项.查询值（「q」），「1」）；

测试.相等（首项.正文文字（），「h」）；

测试.相等（首项.正文模式（），「定长」）；

测试.断言（首项.保持连接（），「HTTP/1.1 默认保持连接」）；

测试.断言（会话.可复用（），「首项之后连接仍可复用」）；

会话.发送临时响应（首项，HTTP.HTTP响应（103，字节.从文字（「」））.设首部（「link」，「</style.css>; rel=preload」））；

会话.发送响应（首项，HTTP.文字响应（「one」））；

测试.相等（会话.响应数（），1）；

定 次项 为 会话.读取请求（）；

测试.相等（次项.方法，「POST」）；

测试.相等（次项.路径，「/two」）；

测试.相等（次项.正文文字（），「W」）；

测试.相等（次项.正文模式（），「分块」）；

测试.相等（次项.尾部值（「X-Check」），「ok」）；

测试.断言（次项.必须关闭（），「Connection: close 必须终止复用」）；

测试.相等（会话.请求数（），2）；

测试.相等（会话.读取下一请求（），空）；

会话.发送响应（次项，HTTP.文字响应（「two」）.设分块（2））；

测试.相等（会话.响应数（），2）；

测试.断言（非 会话.可复用（），「关闭请求之后连接不可复用」）；

会话.关闭（）；

套接字.关闭（监听器）；

言 「言讯持久连接通过」；
