引「../src/言序HTTP.yx」为 HTTP；

引「标准:套接字」为 套接字；

引「标准:环境」为 环境；

引「标准:测试」为 测试；

引「标准:字节」为 字节；

定 参数列：列<文> 为 环境.参数（）；

测试.相等（长度（参数列），1）；

定 监听器：套接字 为 套接字.TCP监听（参数列【0】）；

定 已接受：典 为 套接字.接受（监听器，5000）；

定 连接：套接字 为 已接受【「套接字」】；

定 会话 为 HTTP.连接会话为（连接，HTTP.限制（）.设读取（5000，8192，65536，1）.设会话（8））；

定 请求 为 会话.读取请求（）；

测试.相等（请求.路径，「/socket」）；

测试.相等（会话.缓冲字节数（），6）；

定 通道 为 会话.升级WebSocket协议（请求，「chat」）；

测试.断言（会话.已升级（），「HTTP 连接须标记为已升级」）；

测试.断言（非 会话.可复用（），「升级后的连接不得继续解析 HTTP」）；

测试.相等（通道.缓冲字节数（），6）；

定 首读：典 为 通道.接收字节（2）；

测试.相等（通道.缓冲字节数（），4）；

定 次读：典 为 通道.接收字节（4）；

定 前缀：字节串 为 字节.从数列（【129，128，1，2，3，4】）；

定 所读：字节串 为 字节.拼接（首读【「数据」】，次读【「数据」】）；

测试.相等（所读，前缀）；

测试.相等（首读【「已结束」】，假）；

测试.相等（次读【「已结束」】，假）；

测试.相等（通道.缓冲字节数（），0）；

通道.发送字节（所读）；

通道.关闭（）；

套接字.关闭（监听器）；

言 「言讯升级通过」；
