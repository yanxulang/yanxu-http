引「../src/言序HTTP.yx」为 HTTP；

引「标准:字节」为 字节；

引「标准:数学」为 数学；

法 数码文（值：数）：文 则
    若 （值 等于 0） 则
        归 「0」；
    终
    若 （值 等于 1） 则
        归 「1」；
    终
    若 （值 等于 2） 则
        归 「2」；
    终
    若 （值 等于 3） 则
        归 「3」；
    终
    若 （值 等于 4） 则
        归 「4」；
    终
    若 （值 等于 5） 则
        归 「5」；
    终
    若 （值 等于 6） 则
        归 「6」；
    终
    若 （值 等于 7） 则
        归 「7」；
    终
    若 （值 等于 8） 则
        归 「8」；
    终
    若 （值 等于 9） 则
        归 「9」；
    终
    抛 「示例数码越界」；
终

法 整数文（值：数）：文 则
    若 （值 等于 0） 则
        归 「0」；
    终
    令 余值：数 为 值；
    令 结果：文 为 「」；
    当 （余值 大于 0） 则
        定 商：数 为 数学.下取整（（余值 除 10））；
        置 结果 为 （数码文（（余值 减 （商 乘 10））） 加 结果）；
        置 余值 为 商；
    终
    归 结果；
终

法 请求（内容类型：文，正文：文） 则
    定 正文字节：字节串 为 字节.从文字（正文）；
    定 首部：文 为 （（（「POST / HTTP/1.1\nHost: localhost\nContent-Type: 」 加 内容类型） 加 「\nContent-Length: 」） 加 整数文（字节.长度（正文字节）））；
    归 HTTP.解析请求（首部，正文字节）；
终

定 表单 为 请求（「application/x-www-form-urlencoded」，「name=%E8%A8%80%E5%BA%8F&tag=a&tag=b」）.表单（）；

言 表单【「name」】【0】；

言 长度（表单【「tag」】）；

定 多部正文：文 为 「--demo\nContent-Disposition: form-data; name="title"\n\n指南\n--demo\nContent-Disposition: form-data; name="file"; filename="notes.txt"\nContent-Type: text/plain; charset=utf-8\n\nhello\n--demo--\n」；

定 多部 为 请求（「multipart/form-data; boundary=demo」，多部正文）.Multipart（）；

言 多部.字段值（「title」）；

言 多部.文件列（「file」）【0】.安全文件名（）；
