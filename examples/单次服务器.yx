引「../src/言序HTTP.yx」为 HTTP；
引「标准:套接字」为 套接字；

定 监听器：套接字 为 套接字.TCP监听（「127.0.0.1:19091」）；
定 已接受：典 为 套接字.接受（监听器，5000）；
定 连接：套接字 为 已接受【「套接字」】；

试 则
    定 请求 为 HTTP.读取请求（连接）；
    定 响应 为 HTTP.JSON响应（{「method」：请求.方法，「path」：请求.路径，「body」：请求.正文文字（）}）；
    HTTP.发送响应（连接，响应）；
救 所误 则
    HTTP.发送响应（连接，HTTP.状态响应（400，「Bad Request」））；
终

套接字.关闭（连接）；
套接字.关闭（监听器）；
