引「../src/言序HTTP.yx」为 HTTP；
引「标准:字节」为 字节；

定 请求 为 HTTP.解析请求（
    「GET /articles?page=1 HTTP/1.1\r\nHost: localhost\r\nCookie: theme=dark」，
    字节.从文字（「」）
）；

言 请求.方法；
言 请求.路径；
言 请求.查询值（「page」）；
言 请求.Cookie值（「theme」）；
言 字节.转文字（HTTP.JSON响应（{「ok」：真}）.正文）；
