# 安全策略

## 支持版本

| 版本线 | 状态 |
| --- | --- |
| `1.0.x` | 接收安全修复 |
| `0.1.x` | 已停止支持，请迁移到 1.0 |

## 私下报告

请通过 GitHub Security Advisory 私下报告漏洞。报告应包含受影响版本、最小复现、预期安全边界和已知缓解方式；不要在公开 Issue 中披露可利用细节，也不要附带真实令牌、Cookie、数据库密码或个人数据。

维护者会先确认影响范围，再协调修复、回归测试和披露时间。安全修复发布后会在更新记录和 GitHub Release 中说明受影响版本与升级建议。

## 部署责任

`yanxu-http`提供 HTTP/1.1 解析与连接状态机，不提供 TLS 终止、并发隔离、代理头信任、访问控制、速率限制、审计留存或进程级资源隔离。面向公网时应置于经过维护的反向代理后，并在应用层配置请求并发、身份认证、日志脱敏和超时策略。

详细威胁模型和不可委托给本库的责任见 [docs/SECURITY_MODEL.md](docs/SECURITY_MODEL.md)。
