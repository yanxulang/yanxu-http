# 兼容性

## 言序工具链

| 工具链 | 1.0 状态 | 验证范围 |
| --- | --- | --- |
| 1.1.6 | 最低支持版本 | 格式、静态检查、规格、树/VM 对照、示例、负载、包锁与发布构建 |
| 1.1.8 | 当前稳定基线 | 完整质量门禁与真实 TCP 集成 |

1.0 的清单声明`言序 = ">=1.1.6"`。低于 1.1.6 的工具链不属于支持范围；后续言序稳定版本若出现兼容问题，将按补丁版本修复或在此记录。

## 平台

协议解析、响应编码、表单、Cookie、Host 与 WebSocket 握手均为纯言序实现。CI 在 Ubuntu、macOS 和 Windows 上验证最低工具链；真实 TCP 集成固定使用回环地址，不依赖公网服务。

## 协议矩阵

| 能力 | 状态 | 边界 |
| --- | --- | --- |
| HTTP/1.1 请求 | 支持 | 严格 CRLF、UTF-8 首部、一个合法 Host |
| 定长正文 | 支持 | `Content-Length`必须与字节数一致 |
| 请求分块传输 | 支持 | 只接受单一最终`chunked`，有分块与尾部预算 |
| keep-alive / 流水线 | 支持 | 每连接顺序处理；不提供并发调度 |
| 临时响应 | 支持 | `1xx`（除 101）不完成当前请求 |
| 分块响应 | 支持 | 编码器产生规范十六进制块与终止块 |
| 表单 / Multipart | 支持 | 有界内存解析，不自动落盘 |
| WebSocket | 握手边界 | RFC 6455 版本 13；不实现帧与扩展 |
| HTTP/2、HTTP/3 | 不支持 | 调用方应使用其他协议实现或网关 |
| TLS | 不支持 | 由受维护的 TLS 终止层提供 |

## 版本承诺

`1.x`保持既有公开名称、参数意义、稳定错误代码和默认安全不变量。增加可选成员属于向后兼容功能；删除公开 API、放宽会导致解析分歧的规则，或改变现有错误代码，需要新的主版本。

内部模块`Cookie内核.yx`、`表单内核.yx`、`路由内核.yx`和`WebSocket内核.yx`不是包默认导出，不承诺直接导入兼容性。
