# 更新记录

## 1.0.0 - 2026-07-16

### 新增

- 加入有界`HTTP限制`、结构化`YANXUN_*`错误与`错误详情`，覆盖请求行、首部、正文、分块、尾部、表单和连接预算。
- 加入严格 Host、原始路径/查询、路径段、方法性质和稳定路由视图。
- 加入请求分块解码、尾部验证、持久连接、流水线、临时响应和定长/分块响应状态机。
- 加入 URL 编码表单与有界 Multipart 字段/文件解析。
- 加入 Cookie 重复值、删除、`SameSite`、`Partitioned`、`__Host-`和`__Secure-`安全契约。
- 加入 RFC 6455 WebSocket 版本 13 握手、Origin 允许列表、子协议选择和无损套接字所有权移交。
- 加入双工具链、三平台、树/VM、真实 TCP、API 漂移、权限、锁文件和新增元数据 CI 门禁。

### 安全修复

- 拒绝冲突 framing、逗号或重复长度、模糊传输编码、折叠首部、非法控制字符和禁止尾部字段。
- 普通响应编码器拒绝 101，确保升级只能经过会话所有权边界。
- 所有解析循环与内存入口具备显式上限，错误后连接不会被误判为可复用。

### 兼容性

- 最低言序提升到 1.1.6；公开稳定版本进入 1.0.0。
- 保留 0.1 的离线请求、响应工厂和一次请求套接字助手；持久连接与升级使用新的`HTTP连接`。
- 1.0 仍只支持 HTTP/1.1；TLS、HTTP/2、HTTP/3 和 WebSocket 帧不在本库范围。

## 0.1.1 - 2026-07-15

- 升级到言序清单格式 2，声明最低言序 1.1.5、默认公开导出、字节码构建目标和原生扩展禁用边界。
- 更新纯言序言包 0.2.0 的命令顺序与言序 1.1.5 CI 基线。

## 0.1.0 - 2026-07-14

- 加入 HTTP/1.1 请求行、首部、查询参数、Cookie 和定长正文解析。
- 加入状态码、响应首部、Set-Cookie 与六类响应助手。
- 加入受首部/正文预算约束的 TCP 请求读取和 `Connection: close` 响应发送。
- 显式拒绝尚未支持的请求分块传输。
