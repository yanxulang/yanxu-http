引「../src/言序HTTP.yx」为 HTTP；

引「标准:测试」为 测试；

引「标准:字节」为 字节；

法 期待错误（任务：法，代码：文）：空 则
    令 已失败：理 为 假；
    试 则
        任务（）；
    救 所误 则
        测试.相等（HTTP.错误详情（所误）【「代码」】，代码）；
        置 已失败 为 真；
    终
    测试.断言（已失败，「非法路由元数据必须按稳定错误分支拒绝」）；
    归 空；
终

定 请求 为 HTTP.解析请求（「GET /articles/%E8%A8%80%E5%BA%8F/?q=a%2Bb&q=two?more HTTP/1.1\nHost: Example.COM:8080」，字节.从文字（「」））；

测试.相等（请求.路径，「/articles/言序/」）；

测试.相等（请求.原始路径（），「/articles/%E8%A8%80%E5%BA%8F/」）；

测试.相等（请求.原始查询（），「q=a%2Bb&q=two?more」）；

定 各段：列 为 请求.路径段（）；

测试.相等（长度（各段），4）；

测试.相等（各段【0】，「」）；

测试.相等（各段【2】，「言序」）；

测试.相等（各段【3】，「」）；

测试.相等（请求.查询全部（「q」）【0】，「a+b」）；

测试.相等（请求.查询全部（「q」）【1】，「two?more」）；

定 主机 为 请求.主机信息（）；

测试.相等（主机.原值，「Example.COM:8080」）；

测试.相等（主机.主机，「example.com」）；

测试.相等（主机.端口，8080）；

测试.相等（主机.是IPv6，假）；

测试.相等（主机.授权区（），「example.com:8080」）；

测试.相等（请求.主机值（），「example.com」）；

测试.相等（请求.端口值（），8080）；

测试.相等（请求.方法是（「GET」），真）；

测试.相等（请求.方法是（「get」），假）；

测试.相等（请求.是安全方法（），真）；

测试.相等（请求.是幂等方法（），真）；

测试.相等（请求.有正文（），假）；

定 视图：典 为 请求.路由视图（）；

测试.相等（视图【「模式版本」】，2）；

测试.相等（视图【「原始路径」】，「/articles/%E8%A8%80%E5%BA%8F/」）；

测试.相等（视图【「路径段」】【2】，「言序」）；

测试.相等（视图【「主机」】，「example.com」）；

测试.相等（视图【「端口」】，8080）；

测试.相等（视图【「授权区」】，「example.com:8080」）；

测试.相等（视图【「正文模式」】，「无」）；

测试.相等（视图【「保持连接」】，真）；

测试.相等（视图【「必须关闭」】，假）；

定 IPv6主机 为 HTTP.解析Host（「[0:0:0:0:0:0:0:1]:8443」）；

测试.相等（IPv6主机.主机，「[::1]」）；

测试.相等（IPv6主机.端口，8443）；

测试.相等（IPv6主机.是IPv6，真）；

测试.相等（IPv6主机.授权区（），「[::1]:8443」）；

定 星号请求 为 HTTP.解析请求（「OPTIONS * HTTP/1.1\nHost: localhost」，字节.从文字（「」））；

测试.相等（星号请求.原始路径（），「*」）；

测试.相等（星号请求.路径段（）【0】，「*」）；

定 PUT请求 为 HTTP.解析请求（「PUT /resource HTTP/1.1\nHost: localhost\nContent-Length: 0」，字节.从文字（「」））；

测试.相等（PUT请求.是安全方法（），假）；

测试.相等（PUT请求.是幂等方法（），真）；

法 下划线主机（）：空 则
    HTTP.解析Host（「bad_host.example」）；
    归 空；
终

期待错误（下划线主机，「YANXUN_HOST_NAME」）；

法 缩写IPv4（）：空 则
    HTTP.解析Host（「127.1」）；
    归 空；
终

期待错误（缩写IPv4，「YANXUN_HOST_CANONICAL」）；

法 空端口（）：空 则
    HTTP.解析Host（「example.com:」）；
    归 空；
终

期待错误（空端口，「YANXUN_HOST_PORT」）；

法 越界端口（）：空 则
    HTTP.解析Host（「example.com:65536」）；
    归 空；
终

期待错误（越界端口，「YANXUN_HOST_PORT」）；

法 未括IPv6（）：空 则
    HTTP.解析Host（「::1」）；
    归 空；
终

期待错误（未括IPv6，「YANXUN_HOST_IPV6」）；

法 尾点主机（）：空 则
    HTTP.解析Host（「example.com.」）；
    归 空；
终

期待错误（尾点主机，「YANXUN_HOST_NAME」）；
