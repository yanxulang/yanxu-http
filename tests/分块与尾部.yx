引「../src/言序HTTP.yx」为 HTTP；

引「标准:测试」为 测试；

引「标准:字节」为 字节；

法 期待分块失败（原文：文，代码：文）：空 则
    令 已失败：理 为 假；
    试 则
        HTTP.解析分块正文（字节.从文字（原文））；
    救 所误 则
        测试.相等（HTTP.错误详情（所误）【「代码」】，代码）；
        置 已失败 为 真；
    终
    测试.断言（已失败，「非法分块正文必须按稳定错误分支拒绝」）；
    归 空；
终

定 带剩余 为 HTTP.解析分块正文（字节.从文字（「4\nWiki\n5;mode=test\npedia\n0\n\nNEXT」））；

测试.相等（字节.转文字（带剩余.正文），「Wikipedia」）；

测试.相等（字节.转文字（带剩余.剩余），「NEXT」）；

测试.相等（长度（键列（带剩余.尾部快照（））），0）；

定 二进制数据 为 字节.拼接（字节.从文字（「2\n」），字节.拼接（字节.从数列（【0，255】），字节.从文字（「\n0\n\n」）））；

定 二进制结果 为 HTTP.解析分块正文（二进制数据）；

测试.相等（二进制结果.正文，字节.从数列（【0，255】））；

定 首部：文 为 「POST /upload HTTP/1.1\nHost: localhost\nTransfer-Encoding: chunked\nTrailer: Digest, X-Meta」；

定 消息 为 HTTP.解析分块请求消息（首部，字节.从文字（「4\nWiki\n5\npedia\n0\nDigest: sha-256=abc\nX-Meta: one\nX-Meta: two\n\nNEXT」））；

测试.相等（消息.请求.正文文字（），「Wikipedia」）；

测试.相等（消息.请求.内容长度，9）；

测试.相等（消息.请求.正文模式（），「分块」）；

测试.相等（消息.请求.尾部值（「digest」），「sha-256=abc」）；

测试.相等（长度（消息.请求.尾部全部（「X-Meta」）），2）；

测试.相等（字节.转文字（消息.剩余），「NEXT」）；

定 精确请求 为 HTTP.解析分块请求（首部，字节.从文字（「1\na\n0\nDigest: ok\n\n」））；

测试.相等（精确请求.正文文字（），「a」）；

令 已拒绝剩余：理 为 假；

试 则
    HTTP.解析分块请求（首部，字节.从文字（「1\na\n0\nDigest: ok\n\nNEXT」））；
救 所误 则
    测试.相等（HTTP.错误详情（所误）【「代码」】，「YANXUN_CHUNK_REMAINDER」）；
    置 已拒绝剩余 为 真；
终

测试.断言（已拒绝剩余，「精确入口必须拒绝管线剩余字节」）；

令 已拒绝未声明尾部：理 为 假；

试 则
    HTTP.解析分块请求（「POST / HTTP/1.1\nHost: localhost\nTransfer-Encoding: chunked」，字节.从文字（「0\nDigest: value\n\n」））；
救 所误 则
    测试.相等（HTTP.错误详情（所误）【「代码」】，「YANXUN_TRAILER_UNDECLARED」）；
    置 已拒绝未声明尾部 为 真；
终

测试.断言（已拒绝未声明尾部，「实际尾部必须预先声明」）；

期待分块失败（「Z\n」，「YANXUN_CHUNK_SIZE」）；

期待分块失败（「4\nWi」，「YANXUN_EOF_CHUNK_DATA」）；

期待分块失败（「4\nWikiX\n0\n\n」，「YANXUN_CHUNK_DATA_END」）；

期待分块失败（「1;=bad\na\n0\n\n」，「YANXUN_CHUNK_EXTENSION」）；

期待分块失败（「1000000\n」，「YANXUN_LIMIT_BODY_BYTES」）；

期待分块失败（「0\nContent-Length: 1\n\n」，「YANXUN_TRAILER_FORBIDDEN」）；

定 少分块限制 为 HTTP.限制（）.设分块（1，128）；

令 已拒绝分块数：理 为 假；

试 则
    HTTP.解析分块正文为（字节.从文字（「1\na\n1\nb\n0\n\n」），少分块限制）；
救 所误 则
    测试.相等（HTTP.错误详情（所误）【「代码」】，「YANXUN_LIMIT_CHUNK_COUNT」）；
    置 已拒绝分块数 为 真；
终

测试.断言（已拒绝分块数，「分块数量预算必须生效」）；

定 小正文限制 为 HTTP.限制（）.设读取（1000，8192，65536，2）；

令 已拒绝累计正文：理 为 假；

试 则
    HTTP.解析分块正文为（字节.从文字（「2\nab\n1\nc\n0\n\n」），小正文限制）；
救 所误 则
    测试.相等（HTTP.错误详情（所误）【「代码」】，「YANXUN_LIMIT_BODY_BYTES」）；
    置 已拒绝累计正文 为 真；
终

测试.断言（已拒绝累计正文，「累计分块正文预算必须生效」）；

定 少尾部限制 为 HTTP.限制（）.设首部项（128，16384，1）；

令 已拒绝尾部数：理 为 假；

试 则
    HTTP.解析分块正文为（字节.从文字（「0\nX-One: 1\nX-Two: 2\n\n」），少尾部限制）；
救 所误 则
    测试.相等（HTTP.错误详情（所误）【「代码」】，「YANXUN_LIMIT_TRAILER_COUNT」）；
    置 已拒绝尾部数 为 真；
终

测试.断言（已拒绝尾部数，「尾部字段数量预算必须生效」）；
