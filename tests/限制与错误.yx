引「../src/言序HTTP.yx」为 HTTP；

引「标准:测试」为 测试；

定 默认限制 为 HTTP.限制（）；

测试.相等（默认限制.超时毫秒，5000）；

测试.相等（默认限制.最大请求行，8192）；

测试.相等（默认限制.最大首部，65536）；

测试.相等（默认限制.最大正文，4194304）；

测试.相等（默认限制.最大分块项，4096）；

测试.相等（默认限制.最大连接请求，100）；

定 收紧限制 为 HTTP.限制（）.设读取（1000，4096，16384，1048576）.设首部项（32，4096，16）.设分块（64，512）.设Multipart为（16，2048）.设会话（8）；

测试.相等（收紧限制.超时毫秒，1000）；

测试.相等（收紧限制.最大首部项，32）；

测试.相等（收紧限制.最大尾部项，16）；

测试.相等（收紧限制.最大Multipart项，16）；

测试.相等（收紧限制.最大Multipart项首部，2048）；

测试.相等（收紧限制.最大连接请求，8）；

定 无效限制 为 HTTP.限制（）；

置 无效限制.最大首部项 为 129；

令 已拒绝：理 为 假；

试 则
    无效限制.验证（）；
救 所误 则
    定 详情：典 为 HTTP.错误详情（所误）；
    测试.相等（详情【「代码」】，「YANXUN_LIMIT_HEADER_COUNT」）；
    测试.断言（（详情【「源代码」】 不等于 「」），「结构化错误必须保留运行时代码」）；
    测试.断言（（详情【「消息」】 不等于 「」），「结构化错误必须保留说明」）；
    置 已拒绝 为 真；
终

测试.断言（已拒绝，「越界首部项预算必须失败」）；

测试.相等（HTTP.结果模式版本，2）；

测试.相等（HTTP.言据媒体类型，「application/vnd.yanxu.yanju」）；

测试.相等（HTTP.状态切换协议，101）；

测试.相等（HTTP.状态首部过大，431）；
