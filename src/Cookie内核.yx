引「标准:文字」为 文字；

引「标准:正则」为 正则；

引「标准:字节」为 字节；

引「标准:数学」为 数学；

法 报错（代码：文，消息：文）：空 则
    抛 （（（「YANXUN_」 加 代码） 加 「：」） 加 消息）；
终

法 首部名合法（名称：文）：理 则
    归 正则.匹配（「^[!#$%&'*+.^_`|~0-9A-Za-z-]+$」，名称）；
终

法 余项联结（各项：列，分隔：文）：文 则
    若 （长度（各项） 小于 2） 则
        归 「」；
    终
    令 结果：列<文> 为 【】；
    令 次：数 为 1；
    当 （次 小于 长度（各项）） 则
        追加（结果，各项【次】）；
        置 次 为 （次 加 1）；
    终
    归 文字.联结（结果，分隔）；
终

公 法 断言名称（名称：文，最大字节：数）：空 则
    若 （名称 等于 「」） 则
        报错（「COOKIE_NAME」，「Cookie 名不可为空」）；
    终
    若 非 首部名合法（名称） 则
        报错（「COOKIE_NAME」，「Cookie 名不是合法 HTTP token」）；
    终
    若 （字节.长度（字节.从文字（名称）） 大于 最大字节） 则
        报错（「LIMIT_COOKIE_NAME_BYTES」，「Cookie 名超过配置预算」）；
    终
    归 空；
终

公 法 断言值（值：文，最大字节：数）：空 则
    若 （字节.长度（字节.从文字（值）） 大于 最大字节） 则
        报错（「LIMIT_COOKIE_VALUE_BYTES」，「Cookie 值超过配置预算」）；
    终
    逐 所字节：数 于 字节.转数列（字节.从文字（值）） 则
        若 （（（（（（所字节 小于 33） 或 （所字节 大于 126）） 或 （所字节 等于 34）） 或 （所字节 等于 44）） 或 （所字节 等于 59）） 或 （所字节 等于 92）） 则
            报错（「COOKIE_VALUE」，「Cookie 值须为安全 ASCII cookie-octet」）；
        终
    终
    归 空；
终

公 法 解析请求Cookie（原文：文，最大首部字节：数，最大项：数，最大名字节：数，最大值字节：数）：列 则
    若 （字节.长度（字节.从文字（原文）） 大于 最大首部字节） 则
        报错（「LIMIT_COOKIE_HEADER_BYTES」，「Cookie 首部超过配置预算」）；
    终
    令 结果：列 为 【】；
    逐 所项：文 于 文字.分割（原文，「;」） 则
        定 项：文 为 文字.修剪（所项）；
        若 （项 不等于 「」） 则
            若 （长度（结果） 不小于 最大项） 则
                报错（「LIMIT_COOKIE_COUNT」，「Cookie 项超过配置预算」）；
            终
            定 键值：列<文> 为 文字.分割（项，「=」）；
            若 （长度（键值） 小于 2） 则
                报错（「COOKIE_PAIR」，「Cookie 项必须包含等号」）；
            终
            定 名称：文 为 键值【0】；
            定 值：文 为 余项联结（键值，「=」）；
            断言名称（名称，最大名字节）；
            断言值（值，最大值字节）；
            追加（结果，{「名称」：名称，「值」：值}）；
        终
    终
    归 结果；
终

公 法 断言路径（路径：文，最大字节：数）：空 则
    若 （路径 等于 「」） 则
        归 空；
    终
    若 非 文字.始于（路径，「/」） 则
        报错（「COOKIE_PATH」，「Cookie Path 必须为空或以斜线开头」）；
    终
    若 （字节.长度（字节.从文字（路径）） 大于 最大字节） 则
        报错（「LIMIT_COOKIE_PATH_BYTES」，「Cookie Path 超过配置预算」）；
    终
    逐 所值：数 于 字节.转数列（字节.从文字（路径）） 则
        若 （（（所值 小于 32） 或 （所值 大于 126）） 或 （所值 等于 59）） 则
            报错（「COOKIE_PATH」，「Cookie Path 只接受分号以外的可见 ASCII」）；
        终
    终
    归 空；
终

公 法 规范域名（域名：文，最大字节：数）：文 则
    若 （文字.修剪（域名） 不等于 域名） 则
        报错（「COOKIE_DOMAIN」，「Cookie Domain 不得含首尾空白」）；
    终
    令 结果：文 为 文字.小写（域名）；
    若 文字.始于（结果，「.」） 则
        置 结果 为 结果【1：】；
    终
    若 （（结果 等于 「」） 或 （字节.长度（字节.从文字（结果）） 大于 最大字节）） 则
        报错（「LIMIT_COOKIE_DOMAIN_BYTES」，「Cookie Domain 为空或超过配置预算」）；
    终
    若 非 正则.匹配（「^[A-Za-z0-9]([A-Za-z0-9-]{0,61}[A-Za-z0-9])?([.][A-Za-z0-9]([A-Za-z0-9-]{0,61}[A-Za-z0-9])?)*$」，结果） 则
        报错（「COOKIE_DOMAIN」，「Cookie Domain 必须是规范 ASCII 域名」）；
    终
    若 正则.匹配（「^[0-9]+[.][0-9]+[.][0-9]+[.][0-9]+$」，结果） 则
        报错（「COOKIE_DOMAIN_IP」，「Cookie Domain 不接受 IP 地址」）；
    终
    归 结果；
终

公 法 断言最大秒（秒：数，最大绝对值：数）：空 则
    若 （数学.下取整（秒） 不等于 秒） 则
        报错（「COOKIE_MAX_AGE」，「Cookie Max-Age 必须是整数」）；
    终
    若 （（秒 小于 减 最大绝对值） 或 （秒 大于 最大绝对值）） 则
        报错（「LIMIT_COOKIE_MAX_AGE」，「Cookie Max-Age 超过配置预算」）；
    终
    归 空；
终

公 法 规范同站（策略：文）：文 则
    定 小写：文 为 文字.小写（策略）；
    若 （小写 等于 「strict」） 则
        归 「Strict」；
    终
    若 （小写 等于 「lax」） 则
        归 「Lax」；
    终
    若 （小写 等于 「none」） 则
        归 「None」；
    终
    报错（「COOKIE_SAME_SITE」，「SameSite 只接受 Strict、Lax 或 None」）；
终

公 法 断言安全属性（名称：文，路径：文，域名：文?，安全：理，同站：文，分区：理）：空 则
    定 规范策略：文 为 规范同站（同站）；
    若 （（规范策略 等于 「None」） 且 非 安全） 则
        报错（「COOKIE_SAME_SITE_SECURE」，「SameSite=None 必须同时启用 Secure」）；
    终
    若 （分区 且 非 安全） 则
        报错（「COOKIE_PARTITIONED_SECURE」，「Partitioned 必须同时启用 Secure」）；
    终
    若 （文字.始于（名称，「__Secure-」） 且 非 安全） 则
        报错（「COOKIE_PREFIX_SECURE」，「__Secure- Cookie 必须启用 Secure」）；
    终
    若 文字.始于（名称，「__Host-」） 则
        若 （（非 安全 或 （路径 不等于 「/」）） 或 （域名 是 文）） 则
            报错（「COOKIE_PREFIX_HOST」，「__Host- Cookie 必须启用 Secure、使用 Path=/ 且省略 Domain」）；
        终
    终
    归 空；
终
