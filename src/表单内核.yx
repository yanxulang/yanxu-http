引「标准:文字」为 文字；

引「标准:正则」为 正则；

引「标准:编码」为 编码；

引「标准:字节」为 字节；

法 报错（代码：文，消息：文）：空 则
    抛 （（（「YANXUN_」 加 代码） 加 「：」） 加 消息）；
终

法 首部名合法（名称：文）：理 则
    归 正则.匹配（「^[!#$%&'*+.^_`|~0-9A-Za-z-]+$」，名称）；
终

法 断言首部（名称：文，值：文）：空 则
    若 非 首部名合法（名称） 则
        报错（「HEADER_NAME」，「首部名不合法」）；
    终
    逐 所值：数 于 字节.转数列（字节.从文字（值）） 则
        若 （（（所值 等于 0） 或 （所值 等于 10）） 或 （所值 等于 13）） 则
            报错（「HEADER_VALUE」，「首部值不得含 NUL、CR 或 LF」）；
        终
        若 （（（所值 小于 32） 且 （所值 不等于 9）） 或 （所值 等于 127）） 则
            报错（「HEADER_VALUE」，「首部值不得含制表符以外的控制字节」）；
        终
    终
    归 空；
终

法 含换行或零字节（值：文）：理 则
    逐 所值：数 于 字节.转数列（字节.从文字（值）） 则
        若 （（（所值 等于 0） 或 （所值 等于 10）） 或 （所值 等于 13）） 则
            归 真；
        终
    终
    归 假；
终

法 余项联结（各项：列，分隔：文）：文 则
    若 （长度（各项） 小于 2） 则
        归 「」；
    终
    令 结果：列<文> 为 【】；
    令 次：数 为 1；
    当 （次 小于 长度（各项）） 则
        追加（结果，各项【次】）；
        置 次 为 （次 加 1）；
    终
    归 文字.联结（结果，分隔）；
终

法 数码值（字：文）：数 则
    若 （字 等于 「0」） 则
        归 0；
    终
    若 （字 等于 「1」） 则
        归 1；
    终
    若 （字 等于 「2」） 则
        归 2；
    终
    若 （字 等于 「3」） 则
        归 3；
    终
    若 （字 等于 「4」） 则
        归 4；
    终
    若 （字 等于 「5」） 则
        归 5；
    终
    若 （字 等于 「6」） 则
        归 6；
    终
    若 （字 等于 「7」） 则
        归 7；
    终
    若 （字 等于 「8」） 则
        归 8；
    终
    若 （字 等于 「9」） 则
        归 9；
    终
    归 减 1；
终

法 十六值（字：文）：数 则
    定 十进制：数 为 数码值（字）；
    若 （十进制 不小于 0） 则
        归 十进制；
    终
    定 小写：文 为 文字.小写（字）；
    若 （小写 等于 「a」） 则
        归 10；
    终
    若 （小写 等于 「b」） 则
        归 11；
    终
    若 （小写 等于 「c」） 则
        归 12；
    终
    若 （小写 等于 「d」） 则
        归 13；
    终
    若 （小写 等于 「e」） 则
        归 14；
    终
    若 （小写 等于 「f」） 则
        归 15；
    终
    归 减 1；
终

法 分割参数段（原文：文，错误代码：文）：列 则
    断言首部（「content-type」，原文）；
    令 各段：列<文> 为 【】；
    令 当前：文 为 「」；
    令 引用中：理 为 假；
    令 转义中：理 为 假；
    逐 所字：文 于 文字.字符列（原文） 则
        若 转义中 则
            置 当前 为 （当前 加 所字）；
            置 转义中 为 假；
        否则
            若 （引用中 且 （所字 等于 「\\」）） 则
                置 当前 为 （当前 加 所字）；
                置 转义中 为 真；
            否则
                若 （所字 等于 「"」） 则
                    置 当前 为 （当前 加 所字）；
                    置 引用中 为 非 引用中；
                否则
                    若 （（所字 等于 「;」） 且 非 引用中） 则
                        追加（各段，文字.修剪（当前））；
                        置 当前 为 「」；
                    否则
                        置 当前 为 （当前 加 所字）；
                    终
                终
            终
        终
    终
    若 （引用中 或 转义中） 则
        报错（错误代码，「参数引用未闭合」）；
    终
    追加（各段，文字.修剪（当前））；
    逐 所段：文 于 各段 则
        若 （所段 等于 「」） 则
            报错（错误代码，「参数列表不得含空段」）；
        终
    终
    归 各段；
终

法 解参数值（原值：文，错误代码：文）：文 则
    若 （原值 等于 「」） 则
        报错（错误代码，「参数值不可为空」）；
    终
    定 各字：列<文> 为 文字.字符列（原值）；
    若 （各字【0】 不等于 「"」） 则
        若 非 首部名合法（原值） 则
            报错（错误代码，「未引用参数值必须是 HTTP token」）；
        终
        归 原值；
    终
    若 （（长度（各字） 小于 2） 或 （各字【（长度（各字） 减 1）】 不等于 「"」）） 则
        报错（错误代码，「引用参数值未闭合」）；
    终
    令 结果：文 为 「」；
    令 次：数 为 1；
    当 （次 小于 （长度（各字） 减 1）） 则
        定 所字：文 为 各字【次】；
        若 （所字 等于 「\\」） 则
            若 （（次 加 1） 不小于 （长度（各字） 减 1）） 则
                报错（错误代码，「参数转义缺少后续字符」）；
            终
            置 次 为 （次 加 1）；
            置 结果 为 （结果 加 各字【次】）；
        否则
            若 （所字 等于 「"」） 则
                报错（错误代码，「引用参数内的双引号必须转义」）；
            终
            置 结果 为 （结果 加 所字）；
        终
        置 次 为 （次 加 1）；
    终
    若 含换行或零字节（结果） 则
        报错（错误代码，「参数值不得含 NUL、CR 或 LF」）；
    终
    归 结果；
终

公 法 解析参数化值（原文：文，错误代码：文）：典 则
    定 各段：列 为 分割参数段（原文，错误代码）；
    定 基础：文 为 文字.小写（文字.修剪（各段【0】））；
    若 （基础 等于 「」） 则
        报错（错误代码，「参数化值缺少基础值」）；
    终
    令 参数：典 为 {}；
    令 次：数 为 1；
    当 （次 小于 长度（各段）） 则
        若 （次 大于 32） 则
            报错（「LIMIT_PARAMETER_COUNT」，「参数化首部超过 32 个参数」）；
        终
        定 键值：列<文> 为 文字.分割（各段【次】，「=」）；
        若 （长度（键值） 小于 2） 则
            报错（错误代码，「参数缺少等号」）；
        终
        定 名称：文 为 文字.小写（文字.修剪（键值【0】））；
        若 非 首部名合法（名称） 则
            报错（错误代码，「参数名不是合法 token」）；
        终
        若 有键（参数，名称） 则
            报错（错误代码，「参数名不得重复」）；
        终
        定 原值：文 为 文字.修剪（余项联结（键值，「=」））；
        置 参数【名称】 为 解参数值（原值，错误代码）；
        置 次 为 （次 加 1）；
    终
    归 {「基础」：基础，「参数」：参数}；
终

公 法 解析媒体类型值（原文：文）：典 则
    定 已解析：典 为 解析参数化值（原文，「MEDIA_TYPE」）；
    定 类型段：列<文> 为 文字.分割（已解析【「基础」】，「/」）；
    若 （长度（类型段） 不等于 2） 则
        报错（「MEDIA_TYPE」，「媒体类型必须包含一个主类型和一个子类型」）；
    终
    定 主类型：文 为 类型段【0】；
    定 子类型：文 为 类型段【1】；
    若 （非 首部名合法（主类型） 或 非 首部名合法（子类型）） 则
        报错（「MEDIA_TYPE」，「媒体主类型与子类型必须是 token」）；
    终
    归 {「主类型」：主类型，「子类型」：子类型，「参数」：已解析【「参数」】}；
终

法 表单百分号解码（原文：文）：文 则
    定 各字：列<文> 为 文字.字符列（原文）；
    令 次：数 为 0；
    当 （次 小于 长度（各字）） 则
        若 （各字【次】 等于 「%」） 则
            若 （（次 加 2） 不小于 长度（各字）） 则
                报错（「FORM_PERCENT」，「表单百分号转义必须包含两个十六进制数码」）；
            终
            定 高：数 为 十六值（各字【（次 加 1）】）；
            定 低：数 为 十六值（各字【（次 加 2）】）；
            若 （（高 小于 0） 或 （低 小于 0）） 则
                报错（「FORM_PERCENT」，「表单百分号转义只接受十六进制数码」）；
            终
            若 （（（高 乘 16） 加 低） 等于 0） 则
                报错（「FORM_NUL」，「表单字段不得解码出 NUL」）；
            终
            置 次 为 （次 加 3）；
        否则
            置 次 为 （次 加 1）；
        终
    终
    定 待解码：文 为 文字.替换（原文，「+」，「 」）；
    令 结果：文 为 「」；
    试 则
        置 结果 为 编码.解百分号（待解码）；
    救 所误 则
        报错（「FORM_ENCODING」，「表单百分号内容不是有效 UTF-8」）；
    终
    逐 所值：数 于 字节.转数列（字节.从文字（结果）） 则
        若 （所值 等于 0） 则
            报错（「FORM_NUL」，「表单字段不得含 NUL」）；
        终
    终
    归 结果；
终

公 法 解析表单（原文：文，最大项：数）：典 则
    若 （原文 等于 「」） 则
        归 {}；
    终
    定 各项：列<文> 为 文字.分割（原文，「&」）；
    若 （长度（各项） 大于 最大项） 则
        报错（「LIMIT_FORM_COUNT」，「表单项超过配置预算」）；
    终
    令 结果：典 为 {}；
    逐 所项：文 于 各项 则
        若 （所项 不等于 「」） 则
            定 键值：列<文> 为 文字.分割（所项，「=」）；
            定 名称：文 为 表单百分号解码（键值【0】）；
            定 值：文 为 表单百分号解码（余项联结（键值，「=」））；
            若 （字节.长度（字节.从文字（名称）） 大于 256） 则
                报错（「LIMIT_FORM_NAME_BYTES」，「表单字段名超过 256 字节安全上限」）；
            终
            逐 所值：数 于 字节.转数列（字节.从文字（名称）） 则
                若 （（所值 小于 32） 或 （所值 等于 127）） 则
                    报错（「FORM_NAME」，「表单字段名不得含控制字节」）；
                终
            终
            若 非 有键（结果，名称） 则
                置 结果【名称】 为 【】；
            终
            追加（结果【名称】，值）；
        终
    终
    归 结果；
终

公 法 Multipart边界合法（边界：文）：理 则
    定 各字：列<文> 为 文字.字符列（边界）；
    若 （（长度（各字） 小于 1） 或 （字节.长度（字节.从文字（边界）） 大于 70）） 则
        归 假；
    终
    若 （各字【（长度（各字） 减 1）】 等于 「 」） 则
        归 假；
    终
    定 允许：文 为 「0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ'()+_,-./:=? 」；
    逐 所字：文 于 各字 则
        若 非 包含（允许，所字） 则
            归 假；
        终
    终
    归 真；
终

法 查找字节自（数据：字节串，片：字节串，起点：数）：数? 则
    定 相对：数? 为 字节.查找（字节.切片（数据，起点，字节.长度（数据）），片）；
    若 （相对 是 数） 则
        归 （起点 加 相对）；
    终
    归 空；
终

法 字节始于（数据：字节串，起点：数，片：字节串）：理 则
    若 （（起点 小于 0） 或 （（起点 加 字节.长度（片）） 大于 字节.长度（数据））） 则
        归 假；
    终
    归 （字节.切片（数据，起点，（起点 加 字节.长度（片））） 等于 片）；
终

法 解析项首部（原文：文，最大项首部：数）：典 则
    若 （字节.长度（字节.从文字（原文）） 大于 最大项首部） 则
        报错（「LIMIT_MULTIPART_HEADER_BYTES」，「Multipart 项首部超过配置预算」）；
    终
    定 各行：列<文> 为 文字.分割（原文，「\n」）；
    若 （长度（各行） 大于 128） 则
        报错（「LIMIT_MULTIPART_HEADER_COUNT」，「Multipart 项首部超过 128 项安全上限」）；
    终
    令 结果：典 为 {}；
    逐 行：文 于 各行 则
        若 （行 等于 「」） 则
            报错（「MULTIPART_HEADER_LINE」，「Multipart 项首部不得含空行」）；
        终
        若 （文字.始于（行，「 」） 或 文字.始于（行，「	」）） 则
            报错（「MULTIPART_HEADER_FOLD」，「Multipart 项首部不接受折叠行」）；
        终
        定 键值：列<文> 为 文字.分割（行，「:」）；
        若 （长度（键值） 小于 2） 则
            报错（「MULTIPART_HEADER_LINE」，「Multipart 项首部缺少冒号」）；
        终
        定 名称：文 为 文字.小写（键值【0】）；
        定 值：文 为 文字.修剪（余项联结（键值，「:」））；
        断言首部（名称，值）；
        若 （字节.长度（字节.从文字（名称）） 大于 256） 则
            报错（「LIMIT_HEADER_NAME」，「Multipart 项首部名超过 256 字节安全上限」）；
        终
        若 （字节.长度（字节.从文字（值）） 大于 8192） 则
            报错（「LIMIT_HEADER_VALUE」，「Multipart 单个项首部值超过 8192 字节安全上限」）；
        终
        若 有键（结果，名称） 则
            报错（「MULTIPART_HEADER_DUPLICATE」，「Multipart 项首部不得重复」）；
        终
        置 结果【名称】 为 值；
    终
    归 结果；
终

法 断言元数据（值：文，最大字节：数，错误代码：文，说明：文）：空 则
    若 （字节.长度（字节.从文字（值）） 大于 最大字节） 则
        报错（错误代码，说明）；
    终
    逐 所值：数 于 字节.转数列（字节.从文字（值）） 则
        若 （（所值 小于 32） 或 （所值 等于 127）） 则
            报错（错误代码，「Multipart name 与 filename 不得含控制字节」）；
        终
    终
    归 空；
终

法 建立项（首部原文：文，数据：字节串，最大项首部：数）：典 则
    定 各首部：典 为 解析项首部（首部原文，最大项首部）；
    若 非 有键（各首部，「content-disposition」） 则
        报错（「MULTIPART_DISPOSITION」，「Multipart 项缺少 Content-Disposition」）；
    终
    定 处置：典 为 解析参数化值（各首部【「content-disposition」】，「MULTIPART_DISPOSITION」）；
    若 （处置【「基础」】 不等于 「form-data」） 则
        报错（「MULTIPART_DISPOSITION」，「Multipart 项只接受 form-data 处置」）；
    终
    定 参数：典 为 处置【「参数」】；
    若 非 有键（参数，「name」） 则
        报错（「MULTIPART_NAME」，「Multipart 项缺少 name 参数」）；
    终
    定 名称：文 为 参数【「name」】；
    若 （名称 等于 「」） 则
        报错（「MULTIPART_NAME」，「Multipart 项 name 不可为空」）；
    终
    断言元数据（名称，256，「MULTIPART_NAME」，「Multipart 项 name 超过 256 字节安全上限」）；
    若 有键（参数，「filename*」） 则
        报错（「MULTIPART_FILENAME_EXTENDED」，「Multipart 不接受歧义 filename* 参数」）；
    终
    令 文件名：文? 为 空；
    若 有键（参数，「filename」） 则
        定 所文件名：文 为 参数【「filename」】；
        断言元数据（所文件名，1024，「LIMIT_MULTIPART_FILENAME_BYTES」，「Multipart 文件名超过 1024 字节安全上限」）；
        置 文件名 为 所文件名；
    终
    若 有键（各首部，「content-transfer-encoding」） 则
        报错（「MULTIPART_TRANSFER_ENCODING」，「Multipart 不接受 Content-Transfer-Encoding」）；
    终
    令 内容类型：文? 为 空；
    若 有键（各首部，「content-type」） 则
        解析媒体类型值（各首部【「content-type」】）；
        置 内容类型 为 各首部【「content-type」】；
    终
    归 {「名称」：名称，「文件名」：文件名，「内容类型」：内容类型，「首部」：各首部，「数据」：数据}；
终

法 查找分隔（数据：字节串，标记：字节串，起点：数）：数? 则
    令 当前：数 为 起点；
    定 CRLF：字节串 为 字节.从文字（「\n」）；
    定 关闭：字节串 为 字节.从文字（「--」）；
    当 （当前 不大于 字节.长度（数据）） 则
        定 所位：数? 为 查找字节自（数据，标记，当前）；
        若 （所位 等于 空） 则
            归 空；
        终
        若 （所位 是 数） 则
            定 后位：数 为 （所位 加 字节.长度（标记））；
            若 字节始于（数据，后位，CRLF） 则
                归 所位；
            终
            若 字节始于（数据，后位，关闭） 则
                定 关闭后：数 为 （后位 加 2）；
                若 （（关闭后 等于 字节.长度（数据）） 或 字节始于（数据，关闭后，CRLF）） 则
                    归 所位；
                终
            终
            置 当前 为 （所位 加 1）；
        终
    终
    归 空；
终

法 结束位置（数据：字节串，起点：数）：数 则
    定 CRLF：字节串 为 字节.从文字（「\n」）；
    令 位置：数 为 起点；
    若 字节始于（数据，位置，CRLF） 则
        置 位置 为 （位置 加 2）；
    终
    若 （位置 不等于 字节.长度（数据）） 则
        报错（「MULTIPART_EPILOGUE」，「严格 Multipart 不接受结束边界后的附加数据」）；
    终
    归 位置；
终

公 法 解析Multipart（数据：字节串，边界：文，最大项：数，最大项首部：数）：列 则
    定 分隔：字节串 为 字节.从文字（（「--」 加 边界））；
    定 标记：字节串 为 字节.从文字（（「\n--」 加 边界））；
    定 CRLF：字节串 为 字节.从文字（「\n」）；
    定 关闭：字节串 为 字节.从文字（「--」）；
    定 首部结束：字节串 为 字节.从文字（「\n\n」）；
    若 非 字节始于（数据，0，分隔） 则
        报错（「MULTIPART_PREAMBLE」，「严格 Multipart 必须从首个边界开始」）；
    终
    令 位置：数 为 字节.长度（分隔）；
    若 字节始于（数据，位置，关闭） 则
        结束位置（数据，（位置 加 2））；
        归 【】；
    终
    若 非 字节始于（数据，位置，CRLF） 则
        报错（「MULTIPART_BOUNDARY」，「首个 Multipart 边界缺少 CRLF」）；
    终
    置 位置 为 （位置 加 2）；
    令 各项：列 为 【】；
    当 真 则
        若 （长度（各项） 不小于 最大项） 则
            报错（「LIMIT_MULTIPART_COUNT」，「Multipart 项超过配置预算」）；
        终
        定 首部终点：数? 为 查找字节自（数据，首部结束，位置）；
        若 （首部终点 等于 空） 则
            报错（「EOF_MULTIPART_HEADER」，「Multipart 项首部未完整结束」）；
        终
        若 （首部终点 是 数） 则
            若 （（首部终点 减 位置） 大于 最大项首部） 则
                报错（「LIMIT_MULTIPART_HEADER_BYTES」，「Multipart 项首部超过配置预算」）；
            终
            令 首部原文：文 为 「」；
            试 则
                置 首部原文 为 字节.转文字（字节.切片（数据，位置，首部终点））；
            救 所误 则
                报错（「MULTIPART_HEADER_ENCODING」，「Multipart 项首部不是有效 UTF-8」）；
            终
            定 内容起点：数 为 （首部终点 加 4）；
            定 下个边界：数? 为 查找分隔（数据，标记，内容起点）；
            若 （下个边界 等于 空） 则
                报错（「EOF_MULTIPART_BOUNDARY」，「Multipart 缺少后续或结束边界」）；
            终
            若 （下个边界 是 数） 则
                追加（各项，建立项（首部原文，字节.切片（数据，内容起点，下个边界），最大项首部））；
                置 位置 为 （下个边界 加 字节.长度（标记））；
                若 字节始于（数据，位置，关闭） 则
                    结束位置（数据，（位置 加 2））；
                    归 各项；
                终
                若 非 字节始于（数据，位置，CRLF） 则
                    报错（「MULTIPART_BOUNDARY」，「Multipart 项边界缺少 CRLF 或结束标记」）；
                终
                置 位置 为 （位置 加 2）；
            终
        终
    终
    报错（「INTERNAL」，「Multipart 解析循环异常结束」）；
终
