引「标准:文字」为 文字；

引「标准:正则」为 正则；

引「标准:编码」为 编码；

引「标准:字节」为 字节；

引「标准:套接字」为 套接字库；

引「标准:JSON」为 JSON；

引「标准:数学」为 数学；

引「表单内核.yx」为 表单内核；

引「Cookie内核.yx」为 Cookie内核；

引「路由内核.yx」为 路由内核；

引「WebSocket内核.yx」为 WebSocket内核；

公 定 版本：文 为 「0.1.0」；

公 定 结果模式版本：数 为 2；

公 定 默认超时毫秒：数 为 5000；

公 定 最大超时毫秒：数 为 86400000；

公 定 默认最大请求行字节：数 为 8192；

公 定 最大请求行字节：数 为 8192；

公 定 默认最大首部字节：数 为 65536；

公 定 最大首部数：数 为 128；

公 定 最大首部名字节：数 为 256；

公 定 最大首部值字节：数 为 8192；

公 定 最大Host字节：数 为 512；

公 定 默认最大正文字节：数 为 4194304；

公 定 最大分块数：数 为 4096；

公 定 最大分块行字节：数 为 4096；

公 定 最大尾部字节：数 为 16384；

公 定 最大尾部数：数 为 64；

公 定 最大Cookie数：数 为 256；

公 定 最大Cookie首部字节：数 为 65536；

公 定 最大Cookie名字节：数 为 256；

公 定 最大Cookie值字节：数 为 4096；

公 定 最大Cookie路径字节：数 为 1024；

公 定 最大Cookie域名字节：数 为 253；

公 定 最大Cookie寿命秒：数 为 2147483647；

公 定 最大查询参数数：数 为 1024；

公 定 最大Multipart项数：数 为 256；

公 定 最大Multipart边界字节：数 为 70；

公 定 最大Multipart项首部字节：数 为 16384；

公 定 默认最大连接请求数：数 为 100；

公 定 最大连接请求数：数 为 4096；

公 定 WebSocket版本：文 为 「13」；

公 定 言据媒体类型：文 为 「application/vnd.yanxu.yanju」；

公 定 状态成功：数 为 200；

公 定 状态切换协议：数 为 101；

公 定 状态已创建：数 为 201；

公 定 状态无内容：数 为 204；

公 定 状态重定向：数 为 302；

公 定 状态未修改：数 为 304；

公 定 状态错误请求：数 为 400；

公 定 状态未找到：数 为 404；

公 定 状态方法不允许：数 为 405；

公 定 状态正文过大：数 为 413；

公 定 状态目标过长：数 为 414；

公 定 状态首部过大：数 为 431；

公 定 状态服务器错误：数 为 500；

公 定 状态版本不支持：数 为 505；

法 报错（代码：文，消息：文）：空 则
    抛 （（（「YANXUN_」 加 代码） 加 「：」） 加 消息）；
终

法 找片（原文：文，片：文）：数? 则
    若 （长度（片） 等于 0） 则
        归 0；
    终
    令 位：数 为 0；
    当 （（位 加 长度（片）） 不大于 长度（原文）） 则
        若 （原文【位：（位 加 长度（片））】 等于 片） 则
            归 位；
        终
        置 位 为 （位 加 1）；
    终
    归 空；
终

公 法 错误详情（所误：误）：典 则
    定 消息：文 为 所误.消息；
    定 分隔：数? 为 找片（消息，「：」）；
    令 代码：文 为 「YANXUN_RUNTIME」；
    令 说明：文 为 消息；
    若 （分隔 是 数） 则
        定 候选：文 为 消息【0：分隔】；
        若 正则.匹配（「^YANXUN_[A-Z0-9_]{1,56}$」，候选） 则
            置 代码 为 候选；
            置 说明 为 消息【（分隔 加 1）：】；
        终
    终
    归 {「代码」：代码，「消息」：说明，「源代码」：所误.代码，「类别」：所误.类别，「位置」：所误.位置，「踪迹」：所误.踪迹}；
终

法 是整数范围（值：数，最小：数，最大：数）：理 则
    归 （（（数学.下取整（值） 等于 值） 且 （值 不小于 最小）） 且 （值 不大于 最大））；
终

公 类 HTTP限制 则
    公 域 超时毫秒：数 为 默认超时毫秒；
    公 域 最大请求行：数 为 默认最大请求行字节；
    公 域 最大首部：数 为 默认最大首部字节；
    公 域 最大首部项：数 为 最大首部数；
    公 域 最大正文：数 为 默认最大正文字节；
    公 域 最大分块项：数 为 最大分块数；
    公 域 最大分块行：数 为 最大分块行字节；
    公 域 最大尾部：数 为 最大尾部字节；
    公 域 最大尾部项：数 为 最大尾部数；
    公 域 最大Multipart项：数 为 最大Multipart项数；
    公 域 最大Multipart项首部：数 为 最大Multipart项首部字节；
    公 域 最大连接请求：数 为 默认最大连接请求数；
    公 法 初始化（） 则
        此.验证（）；
    终
    公 法 设读取（超时毫秒：数，最大请求行：数，最大首部：数，最大正文：数）：HTTP限制 则
        置 此.超时毫秒 为 超时毫秒；
        置 此.最大请求行 为 最大请求行；
        置 此.最大首部 为 最大首部；
        置 此.最大正文 为 最大正文；
        归 此.验证（）；
    终
    公 法 设首部项（最大首部项：数，最大尾部：数，最大尾部项：数）：HTTP限制 则
        置 此.最大首部项 为 最大首部项；
        置 此.最大尾部 为 最大尾部；
        置 此.最大尾部项 为 最大尾部项；
        归 此.验证（）；
    终
    公 法 设分块（最大分块项：数，最大分块行：数）：HTTP限制 则
        置 此.最大分块项 为 最大分块项；
        置 此.最大分块行 为 最大分块行；
        归 此.验证（）；
    终
    公 法 设会话（最大连接请求：数）：HTTP限制 则
        置 此.最大连接请求 为 最大连接请求；
        归 此.验证（）；
    终
    公 法 设Multipart（最大项：数）：HTTP限制 则
        置 此.最大Multipart项 为 最大项；
        归 此.验证（）；
    终
    公 法 设Multipart为（最大项：数，最大项首部：数）：HTTP限制 则
        置 此.最大Multipart项 为 最大项；
        置 此.最大Multipart项首部 为 最大项首部；
        归 此.验证（）；
    终
    公 法 验证（）：HTTP限制 则
        若 非 是整数范围（此.超时毫秒，1，最大超时毫秒） 则
            报错（「LIMIT_TIMEOUT」，「超时须为一至 86400000 毫秒的整数」）；
        终
        若 非 是整数范围（此.最大请求行，1，最大请求行字节） 则
            报错（「LIMIT_REQUEST_LINE」，「请求行预算须为一至 8192 字节的整数」）；
        终
        若 非 是整数范围（此.最大首部，此.最大请求行，默认最大首部字节） 则
            报错（「LIMIT_HEADER_BYTES」，「首部预算须不小于请求行且不大于 65536 字节」）；
        终
        若 非 是整数范围（此.最大首部项，1，最大首部数） 则
            报错（「LIMIT_HEADER_COUNT」，「首部项预算须为一至 128 的整数」）；
        终
        若 非 是整数范围（此.最大正文，0，默认最大正文字节） 则
            报错（「LIMIT_BODY_BYTES」，「正文预算须为零至 4194304 字节的整数」）；
        终
        若 非 是整数范围（此.最大分块项，1，最大分块数） 则
            报错（「LIMIT_CHUNK_COUNT」，「分块项预算须为一至 4096 的整数」）；
        终
        若 非 是整数范围（此.最大分块行，1，最大分块行字节） 则
            报错（「LIMIT_CHUNK_LINE」，「分块行预算须为一至 4096 字节的整数」）；
        终
        若 非 是整数范围（此.最大尾部，0，最大尾部字节） 则
            报错（「LIMIT_TRAILER_BYTES」，「尾部预算须为零至 16384 字节的整数」）；
        终
        若 非 是整数范围（此.最大尾部项，0，最大尾部数） 则
            报错（「LIMIT_TRAILER_COUNT」，「尾部项预算须为零至 64 的整数」）；
        终
        若 非 是整数范围（此.最大Multipart项，1，最大Multipart项数） 则
            报错（「LIMIT_MULTIPART_COUNT」，「Multipart 项预算须为一至 256 的整数」）；
        终
        若 非 是整数范围（此.最大Multipart项首部，256，最大Multipart项首部字节） 则
            报错（「LIMIT_MULTIPART_HEADER_BYTES」，「Multipart 项首部预算须为 256 至 16384 字节的整数」）；
        终
        若 非 是整数范围（此.最大连接请求，1，最大连接请求数） 则
            报错（「LIMIT_KEEP_ALIVE_COUNT」，「连接请求预算须为一至 4096 的整数」）；
        终
        归 此；
    终
终

公 法 限制（）：HTTP限制 则
    归 HTTP限制（）；
终

法 复制列（来源：列）：列 则
    令 结果：列 为 【】；
    逐 项 于 来源 则
        追加（结果，项）；
    终
    归 结果；
终

法 复制典（来源：典）：典 则
    令 结果：典 为 {}；
    逐 键 于 键列（来源） 则
        定 值 为 来源【键】；
        若 （值 是 列） 则
            置 结果【键】 为 复制列（值）；
        否则
            置 结果【键】 为 值；
        终
    终
    归 结果；
终

法 数码值（字：文）：数 则
    若 （字 等于 「0」） 则
        归 0；
    终
    若 （字 等于 「1」） 则
        归 1；
    终
    若 （字 等于 「2」） 则
        归 2；
    终
    若 （字 等于 「3」） 则
        归 3；
    终
    若 （字 等于 「4」） 则
        归 4；
    终
    若 （字 等于 「5」） 则
        归 5；
    终
    若 （字 等于 「6」） 则
        归 6；
    终
    若 （字 等于 「7」） 则
        归 7；
    终
    若 （字 等于 「8」） 则
        归 8；
    终
    若 （字 等于 「9」） 则
        归 9；
    终
    归 减 1；
终

法 十进制整数为（原文：文，最大值：数）：数 则
    若 （原文 等于 「」） 则
        报错（「NUMBER_DECIMAL」，「十进制整数不可为空」）；
    终
    令 结果：数 为 0；
    逐 字：文 于 文字.字符列（原文） 则
        定 数码：数 为 数码值（字）；
        若 （数码 小于 0） 则
            报错（「NUMBER_DECIMAL」，「只允许十进制数码」）；
        终
        置 结果 为 （（结果 乘 10） 加 数码）；
        若 （结果 大于 最大值） 则
            报错（「LIMIT_BODY_BYTES」，「正文长度超过配置预算」）；
        终
    终
    归 结果；
终

法 十进制整数（原文：文）：数 则
    归 十进制整数为（原文，默认最大正文字节）；
终

法 数码文（值：数）：文 则
    若 （值 等于 0） 则
        归 「0」；
    终
    若 （值 等于 1） 则
        归 「1」；
    终
    若 （值 等于 2） 则
        归 「2」；
    终
    若 （值 等于 3） 则
        归 「3」；
    终
    若 （值 等于 4） 则
        归 「4」；
    终
    若 （值 等于 5） 则
        归 「5」；
    终
    若 （值 等于 6） 则
        归 「6」；
    终
    若 （值 等于 7） 则
        归 「7」；
    终
    若 （值 等于 8） 则
        归 「8」；
    终
    若 （值 等于 9） 则
        归 「9」；
    终
    抛 「HTTP_NUMBER：数码越界」；
终

法 整数文（值：数）：文 则
    若 （（值 小于 0） 或 （数学.下取整（值） 不等于 值）） 则
        抛 「HTTP_NUMBER：需要非负整数」；
    终
    若 （值 等于 0） 则
        归 「0」；
    终
    令 余值：数 为 值；
    令 结果：文 为 「」；
    当 （余值 大于 0） 则
        定 商：数 为 数学.下取整（（余值 除 10））；
        置 结果 为 （数码文（（余值 减 （商 乘 10））） 加 结果）；
        置 余值 为 商；
    终
    归 结果；
终

法 十六数码文（值：数）：文 则
    若 （值 小于 10） 则
        归 数码文（值）；
    终
    若 （值 等于 10） 则
        归 「a」；
    终
    若 （值 等于 11） 则
        归 「b」；
    终
    若 （值 等于 12） 则
        归 「c」；
    终
    若 （值 等于 13） 则
        归 「d」；
    终
    若 （值 等于 14） 则
        归 「e」；
    终
    若 （值 等于 15） 则
        归 「f」；
    终
    报错（「INTERNAL」，「十六进制数码越界」）；
终

法 十六整数文（值：数）：文 则
    若 非 是整数范围（值，0，默认最大正文字节） 则
        报错（「INTERNAL」，「十六进制整数越界」）；
    终
    若 （值 等于 0） 则
        归 「0」；
    终
    令 余值：数 为 值；
    令 结果：文 为 「」；
    当 （余值 大于 0） 则
        定 商：数 为 数学.下取整（（余值 除 16））；
        置 结果 为 （十六数码文（（余值 减 （商 乘 16））） 加 结果）；
        置 余值 为 商；
    终
    归 结果；
终

法 首部名合法（名称：文）：理 则
    归 正则.匹配（「^[!#$%&'*+.^_`|~0-9A-Za-z-]+$」，名称）；
终

法 十六值（字：文）：数 则
    定 十进制：数 为 数码值（字）；
    若 （十进制 不小于 0） 则
        归 十进制；
    终
    定 小写：文 为 文字.小写（字）；
    若 （小写 等于 「a」） 则
        归 10；
    终
    若 （小写 等于 「b」） 则
        归 11；
    终
    若 （小写 等于 「c」） 则
        归 12；
    终
    若 （小写 等于 「d」） 则
        归 13；
    终
    若 （小写 等于 「e」） 则
        归 14；
    终
    若 （小写 等于 「f」） 则
        归 15；
    终
    归 减 1；
终

法 含换行或零字节（值：文）：理 则
    逐 所字节：数 于 字节.转数列（字节.从文字（值）） 则
        若 （（（所字节 等于 0） 或 （所字节 等于 10）） 或 （所字节 等于 13）） 则
            归 真；
        终
    终
    归 假；
终

法 断言首部（名称：文，值：文）：空 则
    若 非 首部名合法（名称） 则
        报错（「HEADER_NAME」，「首部名不合法」）；
    终
    逐 所字节：数 于 字节.转数列（字节.从文字（值）） 则
        若 （（（所字节 等于 0） 或 （所字节 等于 10）） 或 （所字节 等于 13）） 则
            报错（「HEADER_VALUE」，「首部值不得含 NUL、CR 或 LF」）；
        终
        若 （（（所字节 小于 32） 且 （所字节 不等于 9）） 或 （所字节 等于 127）） 则
            报错（「HEADER_VALUE」，「首部值不得含制表符以外的控制字节」）；
        终
    终
    归 空；
终

法 余项联结（各项：列，分隔：文）：文 则
    若 （长度（各项） 小于 2） 则
        归 「」；
    终
    令 结果：列<文> 为 【】；
    令 次：数 为 1；
    当 （次 小于 长度（各项）） 则
        追加（结果，各项【次】）；
        置 次 为 （次 加 1）；
    终
    归 文字.联结（结果，分隔）；
终

法 百分号解码为（原文：文，加号为空格：理，拒绝分隔：理）：文 则
    定 各字：列<文> 为 文字.字符列（原文）；
    令 次：数 为 0；
    当 （次 小于 长度（各字）） 则
        若 （各字【次】 等于 「%」） 则
            若 （（次 加 2） 不小于 长度（各字）） 则
                报错（「TARGET_PERCENT」，「百分号转义必须包含两个十六进制数码」）；
            终
            定 高：数 为 十六值（各字【（次 加 1）】）；
            定 低：数 为 十六值（各字【（次 加 2）】）；
            若 （（高 小于 0） 或 （低 小于 0）） 则
                报错（「TARGET_PERCENT」，「百分号转义只接受十六进制数码」）；
            终
            定 所值：数 为 （（高 乘 16） 加 低）；
            若 （（（所值 等于 0） 或 （所值 等于 10）） 或 （所值 等于 13）） 则
                报错（「TARGET_CONTROL」，「请求目标不得解码出 NUL、CR 或 LF」）；
            终
            若 （拒绝分隔 且 （（所值 等于 47） 或 （所值 等于 92））） 则
                报错（「TARGET_SEPARATOR」，「路径不得使用转义斜线或反斜线」）；
            终
            置 次 为 （次 加 3）；
        否则
            置 次 为 （次 加 1）；
        终
    终
    令 待解码：文 为 原文；
    若 加号为空格 则
        置 待解码 为 文字.替换（原文，「+」，「 」）；
    终
    试 则
        归 编码.解百分号（待解码）；
    救 所误 则
        报错（「TARGET_ENCODING」，「请求目标的百分号内容不是有效 UTF-8」）；
    终
    报错（「INTERNAL」，「请求目标解码状态不一致」）；
终

法 百分号解码（原文：文）：文 则
    归 百分号解码为（原文，真，假）；
终

法 解析目标（方法：文，目标：文）：典 则
    若 （目标 等于 「*」） 则
        若 （方法 不等于 「OPTIONS」） 则
            报错（「TARGET_ASTERISK」，「星号目标只允许 OPTIONS 方法」）；
        终
        归 {「路径」：「*」，「查询」：{}}；
    终
    若 非 文字.始于（目标，「/」） 则
        报错（「TARGET_FORM」，「服务端只接受 origin-form 请求目标」）；
    终
    若 包含（目标，「#」） 则
        报错（「TARGET_FRAGMENT」，「请求目标不得含片段」）；
    终
    定 各部：列<文> 为 文字.分割（目标，「?」）；
    定 路径：文 为 各部【0】；
    令 查询原文：文 为 「」；
    若 （长度（各部） 大于 1） 则
        置 查询原文 为 余项联结（各部，「?」）；
    终
    令 查询：典 为 {}；
    令 查询参数数：数 为 0；
    若 （查询原文 不等于 「」） 则
        逐 所对：文 于 文字.分割（查询原文，「&」） 则
            若 （所对 不等于 「」） 则
                置 查询参数数 为 （查询参数数 加 1）；
                若 （查询参数数 大于 最大查询参数数） 则
                    报错（「LIMIT_QUERY_COUNT」，「查询参数超过 1024 项安全上限」）；
                终
                定 键值：列<文> 为 文字.分割（所对，「=」）；
                定 键：文 为 百分号解码（键值【0】）；
                定 值：文 为 百分号解码（余项联结（键值，「=」））；
                若 非 有键（查询，键） 则
                    置 查询【键】 为 【】；
                终
                追加（查询【键】，值）；
            终
        终
    终
    归 {「路径」：百分号解码为（路径，假，真），「查询」：查询}；
终

公 类 HTTP请求Cookie项 则
    公 只 域 名称：文；
    公 只 域 值：文；
    公 法 初始化（名称：文，值：文） 则
        Cookie内核.断言名称（名称，最大Cookie名字节）；
        Cookie内核.断言值（值，最大Cookie值字节）；
        置 此.名称 为 名称；
        置 此.值 为 值；
    终
终

公 法 解析Cookie项列（原文：文）：列 则
    定 原始项：列 为 Cookie内核.解析请求Cookie（原文，最大Cookie首部字节，最大Cookie数，最大Cookie名字节，最大Cookie值字节）；
    令 结果：列 为 【】；
    逐 所项：典 于 原始项 则
        追加（结果，HTTP请求Cookie项（所项【「名称」】，所项【「值」】））；
    终
    归 结果；
终

公 法 解析Cookie（原文：文）：典 则
    令 结果：典 为 {}；
    逐 所项：HTTP请求Cookie项 于 解析Cookie项列（原文） 则
        若 非 有键（结果，所项.名称） 则
            置 结果【所项.名称】 为 所项.值；
        终
    终
    归 结果；
终

公 类 HTTP主机信息 则
    公 只 域 原值：文；
    公 只 域 主机：文；
    公 只 域 端口：数?；
    公 只 域 是IPv6：理；
    公 法 初始化（原值：文，主机：文，端口：数?，是IPv6：理） 则
        置 此.原值 为 原值；
        置 此.主机 为 主机；
        置 此.端口 为 端口；
        置 此.是IPv6 为 是IPv6；
    终
    公 法 授权区（）：文 则
        定 所端口：数? 为 此.端口；
        若 （所端口 是 数） 则
            归 （（此.主机 加 「:」） 加 整数文（所端口））；
        终
        归 此.主机；
    终
终

公 法 解析Host（原文：文）：HTTP主机信息 则
    定 已解析：典 为 路由内核.解析主机（原文，最大Host字节）；
    归 HTTP主机信息（已解析【「原值」】，已解析【「主机」】，已解析【「端口」】，已解析【「IPv6」】）；
终

公 类 HTTPWebSocket升级响应 则
    公 只 域 接受值：文；
    公 只 域 子协议：文?；
    公 法 初始化（接受值：文，子协议：文?） 则
        WebSocket内核.断言接受值（接受值）；
        若 （子协议 是 文） 则
            WebSocket内核.断言协议（子协议）；
        终
        置 此.接受值 为 接受值；
        置 此.子协议 为 子协议；
    终
    公 法 首部值（名称：文）：文? 则
        定 小名：文 为 文字.小写（名称）；
        若 （小名 等于 「upgrade」） 则
            归 「websocket」；
        终
        若 （小名 等于 「connection」） 则
            归 「Upgrade」；
        终
        若 （小名 等于 「sec-websocket-accept」） 则
            归 此.接受值；
        终
        若 （小名 等于 「sec-websocket-protocol」） 则
            归 此.子协议；
        终
        归 空；
    终
    公 法 编码（）：字节串 则
        令 各行：列<文> 为 【「HTTP/1.1 101 Switching Protocols」，「upgrade: websocket」，「connection: Upgrade」，（「sec-websocket-accept: 」 加 此.接受值）】；
        定 所协议：文? 为 此.子协议；
        若 （所协议 是 文） 则
            追加（各行，（「sec-websocket-protocol: 」 加 所协议））；
        终
        归 字节.从文字（（文字.联结（各行，「\n」） 加 「\n\n」））；
    终
终

公 类 HTTPWebSocket握手 则
    公 只 域 密钥：文；
    公 只 域 接受值：文；
    私 只 域 协议值：列；
    公 只 域 来源：文?；
    公 只 域 扩展原文：文?；
    公 法 初始化（密钥：文，接受值：文，协议值：列，来源：文?，扩展原文：文?） 则
        WebSocket内核.断言密钥（密钥）；
        WebSocket内核.断言接受值（接受值）；
        置 此.密钥 为 密钥；
        置 此.接受值 为 接受值；
        置 此.协议值 为 复制列（协议值）；
        置 此.来源 为 来源；
        置 此.扩展原文 为 扩展原文；
    终
    公 法 协议列（）：列 则
        归 复制列（此.协议值）；
    终
    公 法 提供协议（协议：文）：理 则
        WebSocket内核.断言协议（协议）；
        归 包含（此.协议值，协议）；
    终
    公 法 选择协议（服务器支持：列）：文? 则
        令 已见：列<文> 为 【】；
        逐 所协议：文 于 服务器支持 则
            WebSocket内核.断言协议（所协议）；
            若 包含（已见，所协议） 则
                报错（「WEBSOCKET_SERVER_PROTOCOL_DUPLICATE」，「服务器子协议列表不得重复」）；
            终
            追加（已见，所协议）；
            若 此.提供协议（所协议） 则
                归 所协议；
            终
        终
        归 空；
    终
    公 法 来源允许（允许来源：列）：理 则
        定 所来源：文? 为 此.来源；
        若 （所来源 是 文） 则
            归 包含（允许来源，所来源）；
        终
        归 假；
    终
    公 法 要求来源（允许来源：列）：HTTPWebSocket握手 则
        若 非 此.来源允许（允许来源） 则
            报错（「WEBSOCKET_ORIGIN」，「WebSocket Origin 不在显式允许列表中」）；
        终
        归 此；
    终
    公 法 响应（）：HTTPWebSocket升级响应 则
        归 HTTPWebSocket升级响应（此.接受值，空）；
    终
    公 法 响应协议（子协议：文）：HTTPWebSocket升级响应 则
        若 非 此.提供协议（子协议） 则
            报错（「WEBSOCKET_PROTOCOL_NOT_OFFERED」，「服务器不可选择客户端未提供的子协议」）；
        终
        归 HTTPWebSocket升级响应（此.接受值，子协议）；
    终
终

公 类 HTTP媒体类型 则
    公 只 域 主类型：文；
    公 只 域 子类型：文；
    私 只 域 参数值：典；
    公 法 初始化（主类型：文，子类型：文，参数值：典） 则
        置 此.主类型 为 主类型；
        置 此.子类型 为 子类型；
        置 此.参数值 为 复制典（参数值）；
    终
    公 法 完整类型（）：文 则
        归 （（此.主类型 加 「/」） 加 此.子类型）；
    终
    公 法 匹配（所类型：文）：理 则
        归 （此.完整类型（） 等于 文字.小写（文字.修剪（所类型）））；
    终
    公 法 参数（名称：文）：文? 则
        定 小名：文 为 文字.小写（名称）；
        若 有键（此.参数值，小名） 则
            归 此.参数值【小名】；
        终
        归 空；
    终
    公 法 参数快照（）：典 则
        归 复制典（此.参数值）；
    终
终

公 法 解析媒体类型（原文：文）：HTTP媒体类型 则
    定 已解析：典 为 表单内核.解析媒体类型值（原文）；
    归 HTTP媒体类型（已解析【「主类型」】，已解析【「子类型」】，已解析【「参数」】）；
终

法 安全文件名为（原名：文）：文? 则
    若 （原名 等于 「」） 则
        归 空；
    终
    定 斜线段：列<文> 为 文字.分割（原名，「/」）；
    定 末段：文 为 斜线段【（长度（斜线段） 减 1）】；
    定 反斜线段：列<文> 为 文字.分割（末段，「\\」）；
    定 结果：文 为 反斜线段【（长度（反斜线段） 减 1）】；
    若 （（（结果 等于 「」） 或 （结果 等于 「.」）） 或 （结果 等于 「..」）） 则
        归 空；
    终
    若 （字节.长度（字节.从文字（结果）） 大于 255） 则
        归 空；
    终
    定 各字：列<文> 为 文字.字符列（结果）；
    定 末字：文 为 各字【（长度（各字） 减 1）】；
    若 （（末字 等于 「.」） 或 （末字 等于 「 」）） 则
        归 空；
    终
    定 禁止：文 为 「<>:"/\\|?*」；
    逐 所字：文 于 各字 则
        若 包含（禁止，所字） 则
            归 空；
        终
    终
    定 名称段：列<文> 为 文字.分割（文字.小写（结果），「.」）；
    定 主名：文 为 名称段【0】；
    定 保留名：列<文> 为 【「con」，「prn」，「aux」，「nul」，「com1」，「com2」，「com3」，「com4」，「com5」，「com6」，「com7」，「com8」，「com9」，「lpt1」，「lpt2」，「lpt3」，「lpt4」，「lpt5」，「lpt6」，「lpt7」，「lpt8」，「lpt9」】；
    若 包含（保留名，主名） 则
        归 空；
    终
    归 结果；
终

公 类 HTTPMultipart项 则
    公 只 域 名称：文；
    公 只 域 文件名：文?；
    公 只 域 内容类型：文?；
    私 只 域 首部值：典；
    公 只 域 数据：字节串；
    公 法 初始化（名称：文，文件名：文?，内容类型：文?，首部值：典，数据：字节串） 则
        置 此.名称 为 名称；
        置 此.文件名 为 文件名；
        置 此.内容类型 为 内容类型；
        置 此.首部值 为 复制典（首部值）；
        置 此.数据 为 数据；
    终
    公 法 是文件（）：理 则
        归 （此.文件名 是 文）；
    终
    公 法 安全文件名（）：文? 则
        定 原名：文? 为 此.文件名；
        若 （原名 是 文） 则
            归 安全文件名为（原名）；
        终
        归 空；
    终
    公 法 大小（）：数 则
        归 字节.长度（此.数据）；
    终
    公 法 文字（）：文 则
        定 所内容类型：文? 为 此.内容类型；
        若 （所内容类型 是 文） 则
            定 所媒体：HTTP媒体类型 为 解析媒体类型（所内容类型）；
            定 字符集：文? 为 所媒体.参数（「charset」）；
            若 （字符集 是 文） 则
                若 （文字.小写（字符集） 不等于 「utf-8」） 则
                    报错（「MULTIPART_CHARSET」，「Multipart 项文字只接受 UTF-8 字符集」）；
                终
            终
        终
        试 则
            归 字节.转文字（此.数据）；
        救 所误 则
            报错（「MULTIPART_FIELD_ENCODING」，「Multipart 项数据不是有效 UTF-8」）；
        终
        报错（「INTERNAL」，「Multipart 文字转换边界异常结束」）；
    终
    公 法 首部（名称：文）：文? 则
        定 小名：文 为 文字.小写（名称）；
        若 有键（此.首部值，小名） 则
            归 此.首部值【小名】；
        终
        归 空；
    终
    公 法 首部快照（）：典 则
        归 复制典（此.首部值）；
    终
终

公 类 HTTPMultipart表单 则
    私 只 域 项值：列；
    公 法 初始化（项值：列） 则
        置 此.项值 为 复制列（项值）；
    终
    公 法 项列（）：列 则
        归 复制列（此.项值）；
    终
    公 法 项数（）：数 则
        归 长度（此.项值）；
    终
    公 法 全部（名称：文）：列 则
        令 结果：列 为 【】；
        逐 所项：HTTPMultipart项 于 此.项值 则
            若 （所项.名称 等于 名称） 则
                追加（结果，所项）；
            终
        终
        归 结果；
    终
    公 法 首项（名称：文）：HTTPMultipart项? 则
        逐 所项：HTTPMultipart项 于 此.项值 则
            若 （所项.名称 等于 名称） 则
                归 所项；
            终
        终
        归 空；
    终
    公 法 字段全部（名称：文）：列 则
        令 结果：列<文> 为 【】；
        逐 所项：HTTPMultipart项 于 此.项值 则
            若 （（所项.名称 等于 名称） 且 非 所项.是文件（）） 则
                追加（结果，所项.文字（））；
            终
        终
        归 结果；
    终
    公 法 字段值（名称：文）：文? 则
        定 各值：列 为 此.字段全部（名称）；
        若 （长度（各值） 大于 0） 则
            归 各值【0】；
        终
        归 空；
    终
    公 法 文件列（名称：文）：列 则
        令 结果：列 为 【】；
        逐 所项：HTTPMultipart项 于 此.项值 则
            若 （（所项.名称 等于 名称） 且 所项.是文件（）） 则
                追加（结果，所项）；
            终
        终
        归 结果；
    终
    公 法 字段快照（）：典 则
        令 结果：典 为 {}；
        逐 所项：HTTPMultipart项 于 此.项值 则
            若 非 所项.是文件（） 则
                若 非 有键（结果，所项.名称） 则
                    置 结果【所项.名称】 为 【】；
                终
                追加（结果【所项.名称】，所项.文字（））；
            终
        终
        归 结果；
    终
终

公 类 HTTP请求 则
    公 只 域 方法：文；
    公 只 域 目标：文；
    公 只 域 路径：文；
    公 只 域 版本：文；
    公 只 域 首部：典；
    公 只 域 查询：典；
    公 只 域 Cookie：典；
    公 只 域 正文：字节串；
    公 只 域 内容长度：数；
    私 域 首部列值：典；
    私 域 尾部列值：典；
    私 域 正文模式值：文；
    私 域 保持连接值：理；
    私 域 必须关闭值：理；
    公 法 初始化（方法：文，目标：文，路径：文，版本：文，首部：典，查询：典，Cookie：典，正文：字节串，内容长度：数） 则
        置 此.方法 为 方法；
        置 此.目标 为 目标；
        置 此.路径 为 路径；
        置 此.版本 为 版本；
        置 此.首部 为 复制典（首部）；
        置 此.查询 为 复制典（查询）；
        置 此.Cookie 为 复制典（Cookie）；
        置 此.正文 为 正文；
        置 此.内容长度 为 内容长度；
        置 此.首部列值 为 {}；
        逐 名称 于 键列（首部） 则
            置 此.首部列值【名称】 为 【首部【名称】】；
        终
        置 此.尾部列值 为 {}；
        置 此.正文模式值 为 「无」；
        若 （内容长度 大于 0） 则
            置 此.正文模式值 为 「定长」；
        终
        置 此.保持连接值 为 假；
        置 此.必须关闭值 为 真；
    终
    公 法 载入协议（首部列：典，尾部列：典，正文模式：文，保持连接：理，必须关闭：理）：HTTP请求 则
        置 此.首部列值 为 复制典（首部列）；
        置 此.尾部列值 为 复制典（尾部列）；
        置 此.正文模式值 为 正文模式；
        置 此.保持连接值 为 保持连接；
        置 此.必须关闭值 为 必须关闭；
        归 此；
    终
    公 法 复制协议到（目标请求：HTTP请求）：HTTP请求 则
        归 目标请求.载入协议（此.首部列值，此.尾部列值，此.正文模式值，此.保持连接值，此.必须关闭值）；
    终
    公 法 复制分块协议到（目标请求：HTTP请求，尾部列：典）：HTTP请求 则
        归 目标请求.载入协议（此.首部列值，尾部列，「分块」，此.保持连接值，此.必须关闭值）；
    终
    公 法 首部值（名称：文）：文? 则
        定 小名：文 为 文字.小写（名称）；
        若 有键（此.首部列值，小名） 则
            定 各值：列 为 此.首部列值【小名】；
            若 （长度（各值） 大于 0） 则
                若 （小名 等于 「cookie」） 则
                    归 文字.联结（各值，「; 」）；
                终
                归 文字.联结（各值，「, 」）；
            终
        终
        归 空；
    终
    公 法 首部全部（名称：文）：列 则
        定 小名：文 为 文字.小写（名称）；
        若 有键（此.首部列值，小名） 则
            归 复制列（此.首部列值【小名】）；
        终
        归 【】；
    终
    公 法 首部快照（）：典 则
        归 复制典（此.首部列值）；
    终
    公 法 查询值（名称：文）：文? 则
        若 非 有键（此.查询，名称） 则
            归 空；
        终
        定 各值：列 为 此.查询【名称】；
        若 （长度（各值） 等于 0） 则
            归 空；
        终
        归 各值【0】；
    终
    公 法 查询全部（名称：文）：列 则
        若 有键（此.查询，名称） 则
            归 复制列（此.查询【名称】）；
        终
        归 【】；
    终
    公 法 查询快照（）：典 则
        归 复制典（此.查询）；
    终
    公 法 Cookie值（名称：文）：文? 则
        若 有键（此.Cookie，名称） 则
            归 此.Cookie【名称】；
        终
        归 空；
    终
    公 法 Cookie项列（）：列 则
        定 各首部：列 为 此.首部全部（「cookie」）；
        若 （长度（各首部） 等于 0） 则
            归 【】；
        终
        归 解析Cookie项列（文字.联结（各首部，「; 」））；
    终
    公 法 Cookie全部（名称：文）：列 则
        令 结果：列<文> 为 【】；
        逐 所项：HTTP请求Cookie项 于 此.Cookie项列（） 则
            若 （所项.名称 等于 名称） 则
                追加（结果，所项.值）；
            终
        终
        归 结果；
    终
    公 法 Cookie快照（）：典 则
        归 复制典（此.Cookie）；
    终
    公 法 主机信息（）：HTTP主机信息 则
        定 所值：文? 为 此.首部值（「host」）；
        若 （所值 是 文） 则
            归 解析Host（所值）；
        终
        报错（「HOST」，「请求缺少 Host 首部」）；
    终
    公 法 主机值（）：文 则
        归 此.主机信息（）.主机；
    终
    公 法 端口值（）：数? 则
        归 此.主机信息（）.端口；
    终
    公 法 原始路径（）：文 则
        归 路由内核.解析原始目标（此.目标）【「原始路径」】；
    终
    公 法 原始查询（）：文 则
        归 路由内核.解析原始目标（此.目标）【「原始查询」】；
    终
    公 法 路径段（）：列 则
        归 路由内核.路径分段（此.路径）；
    终
    公 法 方法是（候选：文）：理 则
        归 （此.方法 等于 候选）；
    终
    公 法 是安全方法（）：理 则
        归 路由内核.安全方法（此.方法）；
    终
    公 法 是幂等方法（）：理 则
        归 路由内核.幂等方法（此.方法）；
    终
    公 法 有正文（）：理 则
        归 （字节.长度（此.正文） 大于 0）；
    终
    公 法 WebSocket握手（）：HTTPWebSocket握手 则
        归 解析WebSocket握手（此）；
    终
    公 法 尾部值（名称：文）：文? 则
        定 小名：文 为 文字.小写（名称）；
        若 有键（此.尾部列值，小名） 则
            定 各值：列 为 此.尾部列值【小名】；
            若 （长度（各值） 大于 0） 则
                归 文字.联结（各值，「, 」）；
            终
        终
        归 空；
    终
    公 法 尾部全部（名称：文）：列 则
        定 小名：文 为 文字.小写（名称）；
        若 有键（此.尾部列值，小名） 则
            归 复制列（此.尾部列值【小名】）；
        终
        归 【】；
    终
    公 法 尾部快照（）：典 则
        归 复制典（此.尾部列值）；
    终
    公 法 正文模式（）：文 则
        归 此.正文模式值；
    终
    公 法 保持连接（）：理 则
        归 此.保持连接值；
    终
    公 法 必须关闭（）：理 则
        归 此.必须关闭值；
    终
    公 法 标记必须关闭（）：HTTP请求 则
        置 此.保持连接值 为 假；
        置 此.必须关闭值 为 真；
        归 此；
    终
    公 法 媒体类型（）：HTTP媒体类型? 则
        定 原值：文? 为 此.首部值（「content-type」）；
        若 （原值 是 文） 则
            归 解析媒体类型（原值）；
        终
        归 空；
    终
    公 法 表单为（所限制：HTTP限制）：典 则
        归 解析表单请求为（此，所限制）；
    终
    公 法 表单（）：典 则
        归 解析表单请求为（此，限制（））；
    终
    公 法 Multipart为（所限制：HTTP限制）：HTTPMultipart表单 则
        归 解析Multipart请求为（此，所限制）；
    终
    公 法 Multipart（）：HTTPMultipart表单 则
        归 解析Multipart请求为（此，限制（））；
    终
    公 法 多部表单为（所限制：HTTP限制）：HTTPMultipart表单 则
        归 此.Multipart为（所限制）；
    终
    公 法 多部表单（）：HTTPMultipart表单 则
        归 此.Multipart（）；
    终
    公 法 路由视图（）：典 则
        定 所主机：HTTP主机信息 为 此.主机信息（）；
        归 {「模式版本」：结果模式版本，「方法」：此.方法，「版本」：此.版本，「目标」：此.目标，「原始路径」：此.原始路径（），「原始查询」：此.原始查询（），「路径」：此.路径，「路径段」：此.路径段（），「主机」：所主机.主机，「端口」：所主机.端口，「授权区」：所主机.授权区（），「查询」：此.查询快照（），「首部」：此.首部快照（），「Cookie」：此.Cookie快照（），「尾部」：此.尾部快照（），「内容长度」：此.内容长度，「正文模式」：此.正文模式值，「有正文」：此.有正文（），「保持连接」：此.保持连接值，「必须关闭」：此.必须关闭值}；
    终
    公 法 正文文字（）：文 则
        归 字节.转文字（此.正文）；
    终
终

公 法 解析WebSocket握手（请求：HTTP请求）：HTTPWebSocket握手 则
    定 已解析：典 为 WebSocket内核.解析握手（请求.方法，请求.版本，请求.正文模式（），请求.必须关闭（），请求.首部全部（「connection」），请求.首部全部（「upgrade」），请求.首部全部（「sec-websocket-key」），请求.首部全部（「sec-websocket-version」），请求.首部全部（「sec-websocket-protocol」），请求.首部全部（「origin」），请求.首部全部（「sec-websocket-extensions」））；
    归 HTTPWebSocket握手（已解析【「密钥」】，已解析【「接受值」】，已解析【「协议」】，已解析【「来源」】，已解析【「扩展」】）；
终

法 断言CRLF（原文：文）：空 则
    定 各字节：列<数> 为 字节.转数列（字节.从文字（原文））；
    令 次：数 为 0；
    当 （次 小于 长度（各字节）） 则
        定 所值：数 为 各字节【次】；
        若 （所值 等于 13） 则
            若 （（（次 加 1） 不小于 长度（各字节）） 或 （各字节【（次 加 1）】 不等于 10）） 则
                报错（「LINE_ENDING」，「请求首部中的 CR 必须紧邻 LF」）；
            终
            置 次 为 （次 加 2）；
        否则
            若 （所值 等于 10） 则
                报错（「LINE_ENDING」，「请求首部中的 LF 必须由 CR 引导」）；
            终
            置 次 为 （次 加 1）；
        终
    终
    归 空；
终

法 解析词列（原文：文，错误代码：文）：列 则
    令 结果：列<文> 为 【】；
    逐 原词：文 于 文字.分割（原文，「,」） 则
        定 所词：文 为 文字.修剪（原词）；
        若 （所词 等于 「」） 则
            报错（错误代码，「逗号词列不得含空项」）；
        终
        若 非 首部名合法（所词） 则
            报错（错误代码，「逗号词列只接受 HTTP token」）；
        终
        追加（结果，文字.小写（所词））；
    终
    归 结果；
终

法 词列含（原文：文，所需：文，错误代码：文）：理 则
    归 包含（解析词列（原文，错误代码），文字.小写（所需））；
终

法 断言Host（值：文）：空 则
    解析Host（值）；
    归 空；
终

法 构造请求头为（原文：文，所限制：HTTP限制）：HTTP请求 则
    所限制.验证（）；
    定 首部字节：数 为 字节.长度（字节.从文字（原文））；
    若 （首部字节 大于 所限制.最大首部） 则
        报错（「LIMIT_HEADER_BYTES」，「请求首部超过配置预算」）；
    终
    断言CRLF（原文）；
    定 各行：列<文> 为 文字.分割（原文，「\n」）；
    若 （长度（各行） 小于 2） 则
        报错（「REQUEST_LINE」，「请求缺少 Host 首部」）；
    终
    定 请求行：文 为 各行【0】；
    若 （字节.长度（字节.从文字（请求行）） 大于 所限制.最大请求行） 则
        报错（「LIMIT_REQUEST_LINE」，「请求行超过配置预算」）；
    终
    定 请求项：列<文> 为 文字.分割（请求行，「 」）；
    若 （（（（（长度（请求项） 不等于 3） 或 （文字.联结（请求项，「 」） 不等于 请求行）） 或 （请求项【0】 等于 「」）） 或 （请求项【1】 等于 「」）） 或 （请求项【2】 等于 「」）） 则
        报错（「REQUEST_LINE」，「请求行必须以单个空格分隔方法、目标和版本」）；
    终
    定 方法：文 为 请求项【0】；
    定 目标：文 为 请求项【1】；
    定 所版本：文 为 请求项【2】；
    若 非 首部名合法（方法） 则
        报错（「METHOD」，「方法不是合法 HTTP token」）；
    终
    若 （字节.长度（字节.从文字（方法）） 大于 64） 则
        报错（「LIMIT_METHOD_BYTES」，「方法超过 64 字节安全上限」）；
    终
    若 （所版本 不等于 「HTTP/1.1」） 则
        报错（「VERSION」，「服务端稳定接口只支持 HTTP/1.1」）；
    终
    若 （（长度（各行） 减 1） 大于 所限制.最大首部项） 则
        报错（「LIMIT_HEADER_COUNT」，「请求首部项超过配置预算」）；
    终
    令 首部：典 为 {}；
    令 首部列：典 为 {}；
    令 次：数 为 1；
    当 （次 小于 长度（各行）） 则
        定 行：文 为 各行【次】；
        若 （行 等于 「」） 则
            报错（「HEADER_LINE」，「首部区内不得有空行」）；
        终
        若 （文字.始于（行，「 」） 或 文字.始于（行，「	」）） 则
            报错（「HEADER_FOLD」，「不接受过时折叠首部」）；
        终
        定 键值：列<文> 为 文字.分割（行，「:」）；
        若 （长度（键值） 小于 2） 则
            报错（「HEADER_LINE」，「首部行缺少冒号」）；
        终
        定 原名：文 为 键值【0】；
        定 值：文 为 文字.修剪（余项联结（键值，「:」））；
        断言首部（原名，值）；
        若 （字节.长度（字节.从文字（原名）） 大于 最大首部名字节） 则
            报错（「LIMIT_HEADER_NAME」，「首部名超过 256 字节安全上限」）；
        终
        若 （字节.长度（字节.从文字（值）） 大于 最大首部值字节） 则
            报错（「LIMIT_HEADER_VALUE」，「单个首部值超过 8192 字节安全上限」）；
        终
        定 小名：文 为 文字.小写（原名）；
        若 有键（首部列，小名） 则
            若 （（（小名 等于 「host」） 或 （小名 等于 「content-length」）） 或 （小名 等于 「transfer-encoding」）） 则
                报错（「HEADER_DUPLICATE」，「Host、Content-Length 与 Transfer-Encoding 不得重复」）；
            终
            追加（首部列【小名】，值）；
        否则
            置 首部列【小名】 为 【值】；
        终
        若 （小名 等于 「cookie」） 则
            置 首部【小名】 为 文字.联结（首部列【小名】，「; 」）；
        否则
            置 首部【小名】 为 文字.联结（首部列【小名】，「, 」）；
        终
        置 次 为 （次 加 1）；
    终
    若 非 有键（首部，「host」） 则
        报错（「HOST」，「HTTP/1.1 请求必须包含一个 Host 首部」）；
    终
    断言Host（首部【「host」】）；
    令 正文模式：文 为 「无」；
    令 内容长度：数 为 0；
    若 有键（首部，「transfer-encoding」） 则
        若 有键（首部，「content-length」） 则
            报错（「FRAMING_CONFLICT」，「Transfer-Encoding 与 Content-Length 不得同时出现」）；
        终
        定 各编码：列 为 解析词列（首部【「transfer-encoding」】，「TRANSFER_ENCODING」）；
        若 （（长度（各编码） 不等于 1） 或 （各编码【0】 不等于 「chunked」）） 则
            报错（「TRANSFER_ENCODING」，「请求仅接受单一且最终的 chunked 传输编码」）；
        终
        置 正文模式 为 「分块」；
    否则
        若 有键（首部，「content-length」） 则
            置 内容长度 为 十进制整数为（首部【「content-length」】，所限制.最大正文）；
            置 正文模式 为 「定长」；
        终
    终
    若 有键（首部，「trailer」） 则
        若 （正文模式 不等于 「分块」） 则
            报错（「TRAILER_MODE」，「Trailer 首部只允许分块请求」）；
        终
        解析词列（首部【「trailer」】，「TRAILER_NAME」）；
    终
    令 保持连接：理 为 真；
    若 有键（首部，「connection」） 则
        定 各连接词：列 为 解析词列（首部【「connection」】，「CONNECTION」）；
        若 包含（各连接词，「close」） 则
            置 保持连接 为 假；
        终
    终
    定 目标信息：典 为 解析目标（方法，目标）；
    令 各Cookie：典 为 {}；
    若 有键（首部，「cookie」） 则
        置 各Cookie 为 解析Cookie（首部【「cookie」】）；
    终
    定 请求：HTTP请求 为 HTTP请求（方法，目标，目标信息【「路径」】，所版本，首部，目标信息【「查询」】，各Cookie，字节.从文字（「」），内容长度）；
    归 请求.载入协议（首部列，{}，正文模式，保持连接，非 保持连接）；
终

法 构造请求头（原文：文）：HTTP请求 则
    归 构造请求头为（原文，限制（））；
终

法 附正文（请求：HTTP请求，正文：字节串）：HTTP请求 则
    若 （请求.正文模式（） 等于 「分块」） 则
        报错（「BODY_MODE」，「分块请求必须使用分块正文解码入口」）；
    终
    若 （字节.长度（正文） 不等于 请求.内容长度） 则
        报错（「BODY_LENGTH」，「正文长度与 Content-Length 不一致」）；
    终
    定 完整请求：HTTP请求 为 HTTP请求（请求.方法，请求.目标，请求.路径，请求.版本，请求.首部，请求.查询，请求.Cookie，正文，请求.内容长度）；
    归 请求.复制协议到（完整请求）；
终

公 法 解析请求头（原文：文）：HTTP请求 则
    归 构造请求头（原文）；
终

公 法 解析请求头为（原文：文，所限制：HTTP限制）：HTTP请求 则
    归 构造请求头为（原文，所限制）；
终

公 法 解析请求（首部原文：文，正文：字节串）：HTTP请求 则
    归 附正文（构造请求头（首部原文），正文）；
终

公 法 解析请求为（首部原文：文，正文：字节串，所限制：HTTP限制）：HTTP请求 则
    归 附正文（构造请求头为（首部原文，所限制），正文）；
终

法 查找字节自（数据：字节串，片：字节串，起点：数）：数? 则
    若 （（起点 小于 0） 或 （起点 大于 字节.长度（数据））） 则
        报错（「INTERNAL」，「字节查找起点越界」）；
    终
    定 相对：数? 为 字节.查找（字节.切片（数据，起点，字节.长度（数据）），片）；
    若 （相对 是 数） 则
        归 （起点 加 相对）；
    终
    归 空；
终

法 字节始于（数据：字节串，起点：数，片：字节串）：理 则
    若 （（起点 小于 0） 或 （（起点 加 字节.长度（片）） 大于 字节.长度（数据））） 则
        归 假；
    终
    归 （字节.切片（数据，起点，（起点 加 字节.长度（片））） 等于 片）；
终

法 拼接字节列（各块：列）：字节串 则
    若 （长度（各块） 等于 0） 则
        归 字节.从文字（「」）；
    终
    令 当前：列 为 复制列（各块）；
    当 （长度（当前） 大于 1） 则
        令 下一层：列 为 【】；
        令 次：数 为 0；
        当 （次 小于 长度（当前）） 则
            若 （（次 加 1） 小于 长度（当前）） 则
                追加（下一层，字节.拼接（当前【次】，当前【（次 加 1）】））；
            否则
                追加（下一层，当前【次】）；
            终
            置 次 为 （次 加 2）；
        终
        置 当前 为 下一层；
    终
    归 当前【0】；
终

法 请求媒体类型为（请求：HTTP请求，所需：文）：HTTP媒体类型 则
    定 所类型：HTTP媒体类型? 为 请求.媒体类型（）；
    若 （所类型 是 HTTP媒体类型） 则
        若 所类型.匹配（所需） 则
            归 所类型；
        终
        报错（「UNSUPPORTED_MEDIA_TYPE」，（（「需要 」 加 所需） 加 「 请求正文」））；
    终
    报错（「UNSUPPORTED_MEDIA_TYPE」，「请求缺少 Content-Type」）；
终

公 法 解析表单请求为（请求：HTTP请求，所限制：HTTP限制）：典 则
    所限制.验证（）；
    若 （字节.长度（请求.正文） 大于 所限制.最大正文） 则
        报错（「LIMIT_BODY_BYTES」，「表单正文超过配置预算」）；
    终
    定 所类型：HTTP媒体类型 为 请求媒体类型为（请求，「application/x-www-form-urlencoded」）；
    定 字符集：文? 为 所类型.参数（「charset」）；
    若 （字符集 是 文） 则
        若 （文字.小写（字符集） 不等于 「utf-8」） 则
            报错（「FORM_CHARSET」，「表单只接受 UTF-8 字符集」）；
        终
    终
    令 原文：文 为 「」；
    试 则
        置 原文 为 字节.转文字（请求.正文）；
    救 所误 则
        报错（「FORM_ENCODING」，「表单正文不是有效 UTF-8」）；
    终
    归 表单内核.解析表单（原文，所限制.最大Multipart项）；
终

公 法 解析表单请求（请求：HTTP请求）：典 则
    归 解析表单请求为（请求，限制（））；
终

公 法 解析Multipart请求为（请求：HTTP请求，所限制：HTTP限制）：HTTPMultipart表单 则
    所限制.验证（）；
    若 （字节.长度（请求.正文） 大于 所限制.最大正文） 则
        报错（「LIMIT_BODY_BYTES」，「Multipart 正文超过配置预算」）；
    终
    定 所类型：HTTP媒体类型 为 请求媒体类型为（请求，「multipart/form-data」）；
    定 所边界：文? 为 所类型.参数（「boundary」）；
    若 非 （所边界 是 文） 则
        报错（「MULTIPART_BOUNDARY」，「multipart/form-data 缺少 boundary 参数」）；
    终
    若 （所边界 是 文） 则
        若 非 表单内核.Multipart边界合法（所边界） 则
            报错（「MULTIPART_BOUNDARY」，「Multipart boundary 不符合一至 70 字节安全语法」）；
        终
        定 原始项：列 为 表单内核.解析Multipart（请求.正文，所边界，所限制.最大Multipart项，所限制.最大Multipart项首部）；
        令 各项：列 为 【】；
        逐 原始：典 于 原始项 则
            追加（各项，HTTPMultipart项（原始【「名称」】，原始【「文件名」】，原始【「内容类型」】，原始【「首部」】，原始【「数据」】））；
        终
        归 HTTPMultipart表单（各项）；
    终
    报错（「INTERNAL」，「Multipart boundary 可空状态不一致」）；
终

公 法 解析Multipart请求（请求：HTTP请求）：HTTPMultipart表单 则
    归 解析Multipart请求为（请求，限制（））；
终

法 读取分块行（数据：字节串，起点：数，最大行：数）：典 则
    定 CRLF：字节串 为 字节.从文字（「\n」）；
    定 终点：数? 为 查找字节自（数据，CRLF，起点）；
    若 （终点 等于 空） 则
        报错（「EOF_CHUNK_LINE」，「分块行在 CRLF 前结束」）；
    终
    若 （终点 是 数） 则
        若 （（终点 减 起点） 大于 最大行） 则
            报错（「LIMIT_CHUNK_LINE」，「分块行超过配置预算」）；
        终
        定 行字节：字节串 为 字节.切片（数据，起点，终点）；
        逐 所值：数 于 字节.转数列（行字节） 则
            若 （所值 大于 127） 则
                报错（「CHUNK_LINE_ASCII」，「分块行只接受 ASCII」）；
            终
        终
        归 {「文字」：字节.转文字（行字节），「下一位」：（终点 加 2），「字节数」：（终点 减 起点）}；
    终
    报错（「INTERNAL」，「分块行状态不一致」）；
终

法 引号扩展值合法（值：文）：理 则
    定 各字：列<文> 为 文字.字符列（值）；
    若 （（（长度（各字） 小于 2） 或 （各字【0】 不等于 「"」）） 或 （各字【（长度（各字） 减 1）】 不等于 「"」）） 则
        归 假；
    终
    令 次：数 为 1；
    当 （次 小于 （长度（各字） 减 1）） 则
        定 所字：文 为 各字【次】；
        若 （所字 等于 「\\」） 则
            置 次 为 （次 加 1）；
            若 （次 不小于 （长度（各字） 减 1）） 则
                归 假；
            终
            定 转义字节：列<数> 为 字节.转数列（字节.从文字（各字【次】））；
            若 （（（长度（转义字节） 不等于 1） 或 （转义字节【0】 小于 32）） 或 （转义字节【0】 等于 127）） 则
                归 假；
            终
        否则
            若 （所字 等于 「"」） 则
                归 假；
            终
            定 所字节：列<数> 为 字节.转数列（字节.从文字（所字））；
            若 （（（长度（所字节） 不等于 1） 或 （所字节【0】 小于 32）） 或 （所字节【0】 等于 127）） 则
                归 假；
            终
        终
        置 次 为 （次 加 1）；
    终
    归 真；
终

法 分块大小（原行：文，最大正文：数）：数 则
    定 各段：列<文> 为 文字.分割（原行，「;」）；
    定 大小文：文 为 文字.修剪（各段【0】）；
    若 （（大小文 等于 「」） 或 （长度（大小文） 大于 16）） 则
        报错（「CHUNK_SIZE」，「分块大小须为一至十六个十六进制数码」）；
    终
    令 大小：数 为 0；
    逐 所字：文 于 文字.字符列（大小文） 则
        定 数码：数 为 十六值（所字）；
        若 （数码 小于 0） 则
            报错（「CHUNK_SIZE」，「分块大小只接受十六进制数码」）；
        终
        置 大小 为 （（大小 乘 16） 加 数码）；
        若 （大小 大于 最大正文） 则
            报错（「LIMIT_BODY_BYTES」，「分块大小超过正文配置预算」）；
        终
    终
    令 次：数 为 1；
    当 （次 小于 长度（各段）） 则
        定 扩展：文 为 文字.修剪（各段【次】）；
        若 （扩展 等于 「」） 则
            报错（「CHUNK_EXTENSION」，「分块扩展不得为空」）；
        终
        定 键值：列<文> 为 文字.分割（扩展，「=」）；
        定 名称：文 为 文字.修剪（键值【0】）；
        若 非 首部名合法（名称） 则
            报错（「CHUNK_EXTENSION」，「分块扩展名不是合法 token」）；
        终
        若 （长度（键值） 大于 1） 则
            定 值：文 为 文字.修剪（余项联结（键值，「=」））；
            若 （（值 等于 「」） 或 （非 首部名合法（值） 且 非 引号扩展值合法（值））） 则
                报错（「CHUNK_EXTENSION」，「分块扩展值须为 token 或安全引号文字」）；
            终
        终
        置 次 为 （次 加 1）；
    终
    归 大小；
终

法 尾部名禁止（名称：文）：理 则
    定 小名：文 为 文字.小写（名称）；
    归 包含（【「transfer-encoding」，「content-length」，「host」，「connection」，「trailer」，「upgrade」，「authorization」，「proxy-authorization」，「cookie」，「set-cookie」】，小名）；
终

法 解析尾部行（各行：列，所限制：HTTP限制）：典 则
    若 （长度（各行） 大于 所限制.最大尾部项） 则
        报错（「LIMIT_TRAILER_COUNT」，「尾部字段超过配置预算」）；
    终
    令 结果：典 为 {}；
    逐 行：文 于 各行 则
        若 （（（行 等于 「」） 或 文字.始于（行，「 」）） 或 文字.始于（行，「	」）） 则
            报错（「TRAILER_LINE」，「尾部字段不得为空或折叠」）；
        终
        定 键值：列<文> 为 文字.分割（行，「:」）；
        若 （长度（键值） 小于 2） 则
            报错（「TRAILER_LINE」，「尾部字段缺少冒号」）；
        终
        定 名称：文 为 键值【0】；
        定 值：文 为 文字.修剪（余项联结（键值，「:」））；
        断言首部（名称，值）；
        若 尾部名禁止（名称） 则
            报错（「TRAILER_FORBIDDEN」，「尾部不得改变分帧、路由、连接或认证语义」）；
        终
        定 小名：文 为 文字.小写（名称）；
        若 有键（结果，小名） 则
            追加（结果【小名】，值）；
        否则
            置 结果【小名】 为 【值】；
        终
    终
    归 结果；
终

公 类 HTTP分块结果 则
    公 只 域 正文：字节串；
    公 只 域 尾部列：典；
    公 只 域 已消费：数；
    公 只 域 剩余：字节串；
    公 法 初始化（正文：字节串，尾部列：典，已消费：数，剩余：字节串） 则
        置 此.正文 为 正文；
        置 此.尾部列 为 复制典（尾部列）；
        置 此.已消费 为 已消费；
        置 此.剩余 为 剩余；
    终
    公 法 尾部值（名称：文）：文? 则
        定 小名：文 为 文字.小写（名称）；
        若 有键（此.尾部列，小名） 则
            归 文字.联结（此.尾部列【小名】，「, 」）；
        终
        归 空；
    终
    公 法 尾部快照（）：典 则
        归 复制典（此.尾部列）；
    终
终

公 类 HTTP请求消息结果 则
    公 只 域 请求：HTTP请求；
    公 只 域 已消费：数；
    公 只 域 剩余：字节串；
    公 法 初始化（请求：HTTP请求，已消费：数，剩余：字节串） 则
        置 此.请求 为 请求；
        置 此.已消费 为 已消费；
        置 此.剩余 为 剩余；
    终
终

公 法 解析分块正文为（数据：字节串，所限制：HTTP限制）：HTTP分块结果 则
    所限制.验证（）；
    令 各块：列 为 【】；
    令 总正文：数 为 0；
    令 分块数：数 为 0；
    令 位：数 为 0；
    当 真 则
        定 大小行：典 为 读取分块行（数据，位，所限制.最大分块行）；
        定 大小：数 为 分块大小（大小行【「文字」】，所限制.最大正文）；
        置 位 为 大小行【「下一位」】；
        若 （大小 等于 0） 则
            令 各尾部行：列<文> 为 【】；
            令 尾部字节：数 为 0；
            当 真 则
                定 尾部行：典 为 读取分块行（数据，位，所限制.最大尾部）；
                置 位 为 尾部行【「下一位」】；
                若 （尾部行【「文字」】 等于 「」） 则
                    定 尾部列：典 为 解析尾部行（各尾部行，所限制）；
                    归 HTTP分块结果（拼接字节列（各块），尾部列，位，字节.切片（数据，位，字节.长度（数据）））；
                终
                置 尾部字节 为 （（尾部字节 加 尾部行【「字节数」】） 加 2）；
                若 （尾部字节 大于 所限制.最大尾部） 则
                    报错（「LIMIT_TRAILER_BYTES」，「尾部字段超过配置预算」）；
                终
                追加（各尾部行，尾部行【「文字」】）；
            终
        终
        置 分块数 为 （分块数 加 1）；
        若 （分块数 大于 所限制.最大分块项） 则
            报错（「LIMIT_CHUNK_COUNT」，「分块数量超过配置预算」）；
        终
        置 总正文 为 （总正文 加 大小）；
        若 （总正文 大于 所限制.最大正文） 则
            报错（「LIMIT_BODY_BYTES」，「分块正文总量超过配置预算」）；
        终
        若 （（（位 加 大小） 加 2） 大于 字节.长度（数据）） 则
            报错（「EOF_CHUNK_DATA」，「分块数据未按声明大小完整到达」）；
        终
        追加（各块，字节.切片（数据，位，（位 加 大小）））；
        置 位 为 （位 加 大小）；
        若 非 字节始于（数据，位，字节.从文字（「\n」）） 则
            报错（「CHUNK_DATA_END」，「分块数据后必须紧跟 CRLF」）；
        终
        置 位 为 （位 加 2）；
    终
    报错（「INTERNAL」，「分块解码循环异常结束」）；
终

公 法 解析分块正文（数据：字节串）：HTTP分块结果 则
    归 解析分块正文为（数据，限制（））；
终

法 校验尾部声明（请求：HTTP请求，尾部列：典）：空 则
    若 （长度（键列（尾部列）） 等于 0） 则
        归 空；
    终
    定 声明：文? 为 请求.首部值（「trailer」）；
    若 （声明 等于 空） 则
        报错（「TRAILER_UNDECLARED」，「含尾部字段的请求必须由 Trailer 首部预先声明」）；
    终
    若 （声明 是 文） 则
        定 各声明：列 为 解析词列（声明，「TRAILER_NAME」）；
        逐 名称 于 键列（尾部列） 则
            若 非 包含（各声明，名称） 则
                报错（「TRAILER_UNDECLARED」，「实际尾部字段未在 Trailer 首部中声明」）；
            终
        终
    终
    归 空；
终

法 附分块正文（头请求：HTTP请求，结果：HTTP分块结果）：HTTP请求 则
    校验尾部声明（头请求，结果.尾部列）；
    定 完整请求：HTTP请求 为 HTTP请求（头请求.方法，头请求.目标，头请求.路径，头请求.版本，头请求.首部，头请求.查询，头请求.Cookie，结果.正文，字节.长度（结果.正文））；
    归 头请求.复制分块协议到（完整请求，结果.尾部列）；
终

公 法 解析分块请求消息为（首部原文：文，数据：字节串，所限制：HTTP限制）：HTTP请求消息结果 则
    定 头请求：HTTP请求 为 构造请求头为（首部原文，所限制）；
    若 （头请求.正文模式（） 不等于 「分块」） 则
        报错（「BODY_MODE」，「请求首部没有声明 chunked 传输编码」）；
    终
    定 结果：HTTP分块结果 为 解析分块正文为（数据，所限制）；
    归 HTTP请求消息结果（附分块正文（头请求，结果），结果.已消费，结果.剩余）；
终

公 法 解析分块请求消息（首部原文：文，数据：字节串）：HTTP请求消息结果 则
    归 解析分块请求消息为（首部原文，数据，限制（））；
终

公 法 解析分块请求为（首部原文：文，数据：字节串，所限制：HTTP限制）：HTTP请求 则
    定 结果：HTTP请求消息结果 为 解析分块请求消息为（首部原文，数据，所限制）；
    若 （字节.长度（结果.剩余） 大于 0） 则
        报错（「CHUNK_REMAINDER」，「精确分块请求入口不接受后续管线字节」）；
    终
    归 结果.请求；
终

公 法 解析分块请求（首部原文：文，数据：字节串）：HTTP请求 则
    归 解析分块请求为（首部原文，数据，限制（））；
终

法 状态短语（状态码：数）：文 则
    若 （状态码 等于 100） 则
        归 「Continue」；
    终
    若 （状态码 等于 101） 则
        归 「Switching Protocols」；
    终
    若 （状态码 等于 103） 则
        归 「Early Hints」；
    终
    若 （状态码 等于 200） 则
        归 「OK」；
    终
    若 （状态码 等于 201） 则
        归 「Created」；
    终
    若 （状态码 等于 202） 则
        归 「Accepted」；
    终
    若 （状态码 等于 203） 则
        归 「Non-Authoritative Information」；
    终
    若 （状态码 等于 204） 则
        归 「No Content」；
    终
    若 （状态码 等于 205） 则
        归 「Reset Content」；
    终
    若 （状态码 等于 206） 则
        归 「Partial Content」；
    终
    若 （状态码 等于 301） 则
        归 「Moved Permanently」；
    终
    若 （状态码 等于 302） 则
        归 「Found」；
    终
    若 （状态码 等于 303） 则
        归 「See Other」；
    终
    若 （状态码 等于 304） 则
        归 「Not Modified」；
    终
    若 （状态码 等于 307） 则
        归 「Temporary Redirect」；
    终
    若 （状态码 等于 308） 则
        归 「Permanent Redirect」；
    终
    若 （状态码 等于 400） 则
        归 「Bad Request」；
    终
    若 （状态码 等于 401） 则
        归 「Unauthorized」；
    终
    若 （状态码 等于 403） 则
        归 「Forbidden」；
    终
    若 （状态码 等于 404） 则
        归 「Not Found」；
    终
    若 （状态码 等于 405） 则
        归 「Method Not Allowed」；
    终
    若 （状态码 等于 406） 则
        归 「Not Acceptable」；
    终
    若 （状态码 等于 408） 则
        归 「Request Timeout」；
    终
    若 （状态码 等于 409） 则
        归 「Conflict」；
    终
    若 （状态码 等于 410） 则
        归 「Gone」；
    终
    若 （状态码 等于 412） 则
        归 「Precondition Failed」；
    终
    若 （状态码 等于 413） 则
        归 「Content Too Large」；
    终
    若 （状态码 等于 415） 则
        归 「Unsupported Media Type」；
    终
    若 （状态码 等于 416） 则
        归 「Range Not Satisfiable」；
    终
    若 （状态码 等于 422） 则
        归 「Unprocessable Content」；
    终
    若 （状态码 等于 426） 则
        归 「Upgrade Required」；
    终
    若 （状态码 等于 429） 则
        归 「Too Many Requests」；
    终
    若 （状态码 等于 431） 则
        归 「Request Header Fields Too Large」；
    终
    若 （状态码 等于 500） 则
        归 「Internal Server Error」；
    终
    若 （状态码 等于 501） 则
        归 「Not Implemented」；
    终
    若 （状态码 等于 502） 则
        归 「Bad Gateway」；
    终
    若 （状态码 等于 503） 则
        归 「Service Unavailable」；
    终
    若 （状态码 等于 504） 则
        归 「Gateway Timeout」；
    终
    若 （状态码 等于 505） 则
        归 「HTTP Version Not Supported」；
    终
    归 「Unknown」；
终

法 断言Cookie名（名称：文）：空 则
    Cookie内核.断言名称（名称，最大Cookie名字节）；
    归 空；
终

法 断言Cookie值（值：文）：空 则
    Cookie内核.断言值（值，最大Cookie值字节）；
    归 空；
终

公 类 HTTPCookie 则
    公 只 域 名称：文；
    公 只 域 值：文；
    公 域 路径：文 为 「/」；
    公 域 域名：文? 为 空；
    公 域 最大秒：数? 为 空；
    公 域 安全：理 为 假；
    公 域 仅HTTP：理 为 真；
    公 域 同站：文 为 「Lax」；
    公 域 分区：理 为 假；
    公 法 初始化（名称：文，值：文） 则
        断言Cookie名（名称）；
        断言Cookie值（值）；
        置 此.名称 为 名称；
        置 此.值 为 值；
    终
    公 法 设路径（路径：文）：HTTPCookie 则
        Cookie内核.断言路径（路径，最大Cookie路径字节）；
        置 此.路径 为 路径；
        归 此；
    终
    公 法 设域名（域名：文）：HTTPCookie 则
        置 此.域名 为 Cookie内核.规范域名（域名，最大Cookie域名字节）；
        归 此；
    终
    公 法 清域名（）：HTTPCookie 则
        置 此.域名 为 空；
        归 此；
    终
    公 法 设最大秒（秒：数）：HTTPCookie 则
        Cookie内核.断言最大秒（秒，最大Cookie寿命秒）；
        置 此.最大秒 为 秒；
        归 此；
    终
    公 法 清最大秒（）：HTTPCookie 则
        置 此.最大秒 为 空；
        归 此；
    终
    公 法 删除（）：HTTPCookie 则
        置 此.最大秒 为 0；
        归 此；
    终
    公 法 设安全（启用：理）：HTTPCookie 则
        置 此.安全 为 启用；
        归 此；
    终
    公 法 设仅HTTP（启用：理）：HTTPCookie 则
        置 此.仅HTTP 为 启用；
        归 此；
    终
    公 法 设同站（策略：文）：HTTPCookie 则
        置 此.同站 为 Cookie内核.规范同站（策略）；
        归 此；
    终
    公 法 设分区（启用：理）：HTTPCookie 则
        置 此.分区 为 启用；
        归 此；
    终
    公 法 渲染（）：文 则
        断言Cookie名（此.名称）；
        断言Cookie值（此.值）；
        Cookie内核.断言路径（此.路径，最大Cookie路径字节）；
        定 规范同站：文 为 Cookie内核.规范同站（此.同站）；
        令 规范域名：文? 为 空；
        定 所域名：文? 为 此.域名；
        若 （所域名 是 文） 则
            置 规范域名 为 Cookie内核.规范域名（所域名，最大Cookie域名字节）；
        终
        定 所最大秒：数? 为 此.最大秒；
        若 （所最大秒 是 数） 则
            Cookie内核.断言最大秒（所最大秒，最大Cookie寿命秒）；
        终
        Cookie内核.断言安全属性（此.名称，此.路径，规范域名，此.安全，规范同站，此.分区）；
        令 各项：列<文> 为 【（（此.名称 加 「=」） 加 此.值）】；
        若 （此.路径 不等于 「」） 则
            追加（各项，（「Path=」 加 此.路径））；
        终
        若 （规范域名 是 文） 则
            追加（各项，（「Domain=」 加 规范域名））；
        终
        若 （所最大秒 是 数） 则
            若 （所最大秒 小于 0） 则
                追加（各项，（「Max-Age=-」 加 整数文（减 所最大秒）））；
            否则
                追加（各项，（「Max-Age=」 加 整数文（所最大秒）））；
            终
        终
        若 此.安全 则
            追加（各项，「Secure」）；
        终
        若 此.仅HTTP 则
            追加（各项，「HttpOnly」）；
        终
        追加（各项，（「SameSite=」 加 规范同站））；
        若 此.分区 则
            追加（各项，「Partitioned」）；
        终
        归 文字.联结（各项，「; 」）；
    终
终

法 状态禁止正文（状态码：数）：理 则
    归 （（（（（状态码 不小于 100） 且 （状态码 小于 200）） 或 （状态码 等于 204）） 或 （状态码 等于 205）） 或 （状态码 等于 304））；
终

法 响应禁止分帧（方法：文，状态码：数）：理 则
    若 （（（（状态码 不小于 100） 且 （状态码 小于 200）） 或 （状态码 等于 204）） 或 （状态码 等于 304）） 则
        归 真；
    终
    归 （（方法 等于 「CONNECT」） 且 （（状态码 不小于 200） 且 （状态码 小于 300）））；
终

法 响应只发首部（方法：文，状态码：数）：理 则
    归 （（方法 等于 「HEAD」） 或 响应禁止分帧（方法，状态码））；
终

法 编码分块正文（正文：字节串，分块字节：数）：字节串 则
    若 非 是整数范围（分块字节，1，65536） 则
        报错（「RESPONSE_CHUNK_SIZE」，「响应分块须为一至 65536 字节的整数」）；
    终
    令 各块：列 为 【】；
    令 起点：数 为 0；
    定 正文长度：数 为 字节.长度（正文）；
    当 （起点 小于 正文长度） 则
        定 终点：数 为 数学.最小（正文长度，（起点 加 分块字节））；
        定 所块：字节串 为 字节.切片（正文，起点，终点）；
        追加（各块，字节.从文字（（十六整数文（字节.长度（所块）） 加 「\n」）））；
        追加（各块，所块）；
        追加（各块，字节.从文字（「\n」））；
        置 起点 为 终点；
    终
    追加（各块，字节.从文字（「0\n\n」））；
    归 拼接字节列（各块）；
终

公 类 HTTP响应 则
    公 只 域 状态码：数；
    私 域 首部：典；
    私 域 Cookie列：列；
    私 域 分块字节值：数?；
    私 域 关闭连接值：理；
    公 只 域 正文：字节串；
    公 法 初始化（状态码：数，正文：字节串） 则
        若 （（（状态码 小于 100） 或 （状态码 大于 599）） 或 （数学.下取整（状态码） 不等于 状态码）） 则
            报错（「STATUS」，「状态码须为 100 至 599 的整数」）；
        终
        若 （状态禁止正文（状态码） 且 （字节.长度（正文） 大于 0）） 则
            报错（「RESPONSE_BODY_FORBIDDEN」，「1xx、204、205 与 304 响应不得携带正文」）；
        终
        置 此.状态码 为 状态码；
        置 此.首部 为 {}；
        置 此.Cookie列 为 【】；
        置 此.分块字节值 为 空；
        置 此.关闭连接值 为 假；
        置 此.正文 为 正文；
    终
    公 法 设首部（名称：文，值：文）：HTTP响应 则
        断言首部（名称，值）；
        定 小名：文 为 文字.小写（名称）；
        若 （（（（（小名 等于 「content-length」） 或 （小名 等于 「connection」）） 或 （小名 等于 「set-cookie」）） 或 （小名 等于 「transfer-encoding」）） 或 （小名 等于 「trailer」）） 则
            报错（「RESPONSE_HEADER_MANAGED」，「该首部由响应编码器管理」）；
        终
        若 （非 有键（此.首部，小名） 且 （长度（键列（此.首部）） 不小于 最大首部数）） 则
            报错（「LIMIT_RESPONSE_HEADER_COUNT」，「响应首部项超过 128 项安全上限」）；
        终
        置 此.首部【小名】 为 值；
        归 此；
    终
    公 法 首部值（名称：文）：文? 则
        定 小名：文 为 文字.小写（名称）；
        若 有键（此.首部，小名） 则
            归 此.首部【小名】；
        终
        归 空；
    终
    公 法 添Cookie（所Cookie：HTTPCookie）：HTTP响应 则
        若 （长度（此.Cookie列） 不小于 最大Cookie数） 则
            报错（「LIMIT_COOKIE_COUNT」，「响应 Cookie 超过 256 项安全上限」）；
        终
        追加（此.Cookie列，所Cookie）；
        归 此；
    终
    公 法 首部快照（）：典 则
        归 复制典（此.首部）；
    终
    公 法 Cookie数（）：数 则
        归 长度（此.Cookie列）；
    终
    公 法 设分块（分块字节：数）：HTTP响应 则
        若 状态禁止正文（此.状态码） 则
            报错（「RESPONSE_FRAMING_FORBIDDEN」，「1xx、204、205 与 304 响应不允许分块传输」）；
        终
        若 非 是整数范围（分块字节，1，65536） 则
            报错（「RESPONSE_CHUNK_SIZE」，「响应分块须为一至 65536 字节的整数」）；
        终
        置 此.分块字节值 为 分块字节；
        归 此；
    终
    公 法 设定长（）：HTTP响应 则
        置 此.分块字节值 为 空；
        归 此；
    终
    公 法 分块字节数（）：数? 则
        归 此.分块字节值；
    终
    公 法 设关闭连接（启用：理）：HTTP响应 则
        置 此.关闭连接值 为 启用；
        归 此；
    终
    公 法 应关闭连接（）：理 则
        归 此.关闭连接值；
    终
    公 法 编码为（请求方法：文，保持连接：理）：字节串 则
        若 （非 首部名合法（请求方法） 或 （字节.长度（字节.从文字（请求方法）） 大于 64）） 则
            报错（「METHOD」，「响应上下文中的请求方法不是合法 HTTP token」）；
        终
        若 （此.状态码 等于 状态切换协议） 则
            报错（「UPGRADE_REQUIRED」，「101 响应须通过协议升级边界编码」）；
        终
        若 （状态禁止正文（此.状态码） 且 （字节.长度（此.正文） 大于 0）） 则
            报错（「RESPONSE_BODY_FORBIDDEN」，「1xx、204、205 与 304 响应不得携带正文」）；
        终
        若 （（（请求方法 等于 「CONNECT」） 且 （（此.状态码 不小于 200） 且 （此.状态码 小于 300））） 且 （字节.长度（此.正文） 大于 0）） 则
            报错（「RESPONSE_BODY_FORBIDDEN」，「成功 CONNECT 响应不得携带 HTTP 正文」）；
        终
        令 各行：列<文> 为 【（（（「HTTP/1.1 」 加 整数文（此.状态码）） 加 「 」） 加 状态短语（此.状态码））】；
        逐 名称 于 排序（键列（此.首部）） 则
            断言首部（名称，此.首部【名称】）；
            追加（各行，（（名称 加 「: 」） 加 此.首部【名称】））；
        终
        逐 所Cookie：HTTPCookie 于 此.Cookie列 则
            追加（各行，（「set-cookie: 」 加 所Cookie.渲染（）））；
        终
        定 禁止分帧：理 为 响应禁止分帧（请求方法，此.状态码）；
        定 所分块字节：数? 为 此.分块字节值；
        若 非 禁止分帧 则
            若 （所分块字节 是 数） 则
                追加（各行，「transfer-encoding: chunked」）；
            否则
                追加（各行，（「content-length: 」 加 整数文（字节.长度（此.正文））））；
            终
        终
        定 有效保持：理 为 （保持连接 且 非 此.关闭连接值）；
        若 （（此.状态码 不小于 200） 且 非 有效保持） 则
            追加（各行，「connection: close」）；
        终
        若 （（长度（各行） 减 1） 大于 最大首部数） 则
            报错（「LIMIT_RESPONSE_HEADER_COUNT」，「编码后的响应首部超过 128 项安全上限」）；
        终
        定 首部文：文 为 （文字.联结（各行，「\n」） 加 「\n\n」）；
        定 首部字节：字节串 为 字节.从文字（首部文）；
        若 （字节.长度（首部字节） 大于 默认最大首部字节） 则
            报错（「LIMIT_RESPONSE_HEADER_BYTES」，「编码后的响应首部超过 65536 字节安全上限」）；
        终
        若 响应只发首部（请求方法，此.状态码） 则
            归 首部字节；
        终
        若 （所分块字节 是 数） 则
            归 字节.拼接（首部字节，编码分块正文（此.正文，所分块字节））；
        终
        归 字节.拼接（首部字节，此.正文）；
    终
    公 法 编码（）：字节串 则
        归 此.编码为（「GET」，假）；
    终
终

公 法 Cookie（名称：文，值：文）：HTTPCookie 则
    归 HTTPCookie（名称，值）；
终

公 法 响应字节（状态码：数，正文：字节串，内容类型：文）：HTTP响应 则
    定 响应：HTTP响应 为 HTTP响应（状态码，正文）；
    响应.设首部（「content-type」，内容类型）；
    归 响应；
终

公 法 字节响应（正文：字节串）：HTTP响应 则
    归 响应字节（200，正文，「application/octet-stream」）；
终

公 法 文字响应（正文：文）：HTTP响应 则
    归 响应字节（200，字节.从文字（正文），「text/plain; charset=utf-8」）；
终

公 法 HTML响应（正文：文）：HTTP响应 则
    归 响应字节（200，字节.从文字（正文），「text/html; charset=utf-8」）；
终

公 法 JSON响应（值）：HTTP响应 则
    归 响应字节（200，字节.从文字（JSON.序列化（值）），「application/json; charset=utf-8」）；
终

公 法 言据响应（正文：文）：HTTP响应 则
    归 响应字节（200，字节.从文字（正文），「application/vnd.yanxu.yanju; charset=utf-8」）；
终

公 法 状态响应（状态码：数，正文：文）：HTTP响应 则
    归 响应字节（状态码，字节.从文字（正文），「text/plain; charset=utf-8」）；
终

公 法 重定向响应（地址：文，状态码：数）：HTTP响应 则
    若 （（（（（状态码 不等于 301） 且 （状态码 不等于 302）） 且 （状态码 不等于 303）） 且 （状态码 不等于 307）） 且 （状态码 不等于 308）） 则
        报错（「STATUS」，「重定向状态码不合法」）；
    终
    定 响应：HTTP响应 为 HTTP响应（状态码，字节.从文字（「」））；
    响应.设首部（「location」，地址）；
    归 响应；
终

公 法 WebSocket版本响应（）：HTTP响应 则
    定 响应：HTTP响应 为 HTTP响应（426，字节.从文字（「」））；
    响应.设首部（「sec-websocket-version」，WebSocket版本）；
    归 响应；
终

公 类 HTTP升级通道 则
    私 只 域 所连接：套接字；
    私 域 前缀：字节串；
    私 域 已关闭值：理；
    公 法 初始化（所连接：套接字，前缀：字节串） 则
        置 此.所连接 为 所连接；
        置 此.前缀 为 前缀；
        置 此.已关闭值 为 假；
    终
    私 法 确保开放（）：空 则
        若 此.已关闭值 则
            报错（「UPGRADE_CHANNEL_CLOSED」，「升级通道已经关闭」）；
        终
        归 空；
    终
    公 法 缓冲字节数（）：数 则
        归 字节.长度（此.前缀）；
    终
    公 法 取缓冲（）：字节串 则
        此.确保开放（）；
        定 结果：字节串 为 此.前缀；
        置 此.前缀 为 字节.从文字（「」）；
        归 结果；
    终
    公 法 接收字节为（最大字节：数，超时毫秒：数）：典 则
        此.确保开放（）；
        若 非 是整数范围（最大字节，1，默认最大正文字节） 则
            报错（「LIMIT_UPGRADE_READ_BYTES」，「升级通道单次读取须为一至 4194304 字节的整数」）；
        终
        若 非 是整数范围（超时毫秒，1，最大超时毫秒） 则
            报错（「LIMIT_TIMEOUT」，「升级通道读取超时须为一至 86400000 毫秒的整数」）；
        终
        若 （字节.长度（此.前缀） 大于 0） 则
            定 取量：数 为 数学.最小（最大字节，字节.长度（此.前缀））；
            定 数据：字节串 为 字节.切片（此.前缀，0，取量）；
            置 此.前缀 为 字节.切片（此.前缀，取量，字节.长度（此.前缀））；
            归 {「数据」：数据，「已结束」：假}；
        终
        归 套接字库.接收字节（此.所连接，最大字节，超时毫秒）；
    终
    公 法 接收字节（最大字节：数）：典 则
        归 此.接收字节为（最大字节，默认超时毫秒）；
    终
    公 法 发送字节为（数据：字节串，超时毫秒：数）：数 则
        此.确保开放（）；
        若 非 是整数范围（超时毫秒，1，最大超时毫秒） 则
            报错（「LIMIT_TIMEOUT」，「升级通道发送超时须为一至 86400000 毫秒的整数」）；
        终
        归 套接字库.发送字节（此.所连接，数据，超时毫秒）；
    终
    公 法 发送字节（数据：字节串）：数 则
        归 此.发送字节为（数据，默认超时毫秒）；
    终
    公 法 本地地址（）：文 则
        此.确保开放（）；
        归 套接字库.本地地址（此.所连接）；
    终
    公 法 对端地址（）：文? 则
        此.确保开放（）；
        归 套接字库.对端地址（此.所连接）；
    终
    公 法 设无延迟（启用：理）：HTTP升级通道 则
        此.确保开放（）；
        套接字库.TCP无延迟（此.所连接，启用）；
        归 此；
    终
    公 法 已关闭（）：理 则
        归 此.已关闭值；
    终
    公 法 关闭（）：空 则
        若 非 此.已关闭值 则
            套接字库.关闭（此.所连接）；
            置 此.已关闭值 为 真；
            置 此.前缀 为 字节.从文字（「」）；
        终
        归 空；
    终
终

法 复制限制（来源：HTTP限制）：HTTP限制 则
    来源.验证（）；
    定 结果：HTTP限制 为 HTTP限制（）；
    置 结果.超时毫秒 为 来源.超时毫秒；
    置 结果.最大请求行 为 来源.最大请求行；
    置 结果.最大首部 为 来源.最大首部；
    置 结果.最大首部项 为 来源.最大首部项；
    置 结果.最大正文 为 来源.最大正文；
    置 结果.最大分块项 为 来源.最大分块项；
    置 结果.最大分块行 为 来源.最大分块行；
    置 结果.最大尾部 为 来源.最大尾部；
    置 结果.最大尾部项 为 来源.最大尾部项；
    置 结果.最大Multipart项 为 来源.最大Multipart项；
    置 结果.最大Multipart项首部 为 来源.最大Multipart项首部；
    置 结果.最大连接请求 为 来源.最大连接请求；
    归 结果.验证（）；
终

公 类 HTTP连接 则
    私 只 域 所连接：套接字；
    私 只 域 所限制：HTTP限制；
    私 域 缓冲：字节串；
    私 域 请求数值：数；
    私 域 响应数值：数；
    私 域 可复用值：理；
    私 域 已结束值：理；
    私 域 已关闭值：理；
    私 域 已升级值：理；
    公 法 初始化（所连接：套接字，所限制：HTTP限制） 则
        置 此.所连接 为 所连接；
        置 此.所限制 为 复制限制（所限制）；
        置 此.缓冲 为 字节.从文字（「」）；
        置 此.请求数值 为 0；
        置 此.响应数值 为 0；
        置 此.可复用值 为 真；
        置 此.已结束值 为 假；
        置 此.已关闭值 为 假；
        置 此.已升级值 为 假；
    终
    私 法 确保开放（）：空 则
        若 此.已升级值 则
            报错（「CONNECTION_UPGRADED」，「HTTP 连接已经移交给升级通道」）；
        终
        若 此.已关闭值 则
            报错（「CONNECTION_CLOSED」，「HTTP 连接已经关闭」）；
        终
        归 空；
    终
    私 法 接收更多（最多字节：数）：理 则
        此.确保开放（）；
        若 此.已结束值 则
            归 假；
        终
        若 非 是整数范围（最多字节，1，默认最大正文字节） 则
            报错（「INTERNAL」，「会话读取预算越界」）；
        终
        定 所读：典 为 套接字库.接收字节（此.所连接，最多字节，此.所限制.超时毫秒）；
        定 数据：字节串 为 所读【「数据」】；
        若 （字节.长度（数据） 大于 0） 则
            置 此.缓冲 为 字节.拼接（此.缓冲，数据）；
        终
        若 所读【「已结束」】 则
            置 此.已结束值 为 真；
        终
        归 （字节.长度（数据） 大于 0）；
    终
    私 法 读取字节数（数量：数，结束代码：文）：字节串 则
        若 非 是整数范围（数量，0，默认最大正文字节） 则
            报错（「INTERNAL」，「会话精确读取预算越界」）；
        终
        若 （数量 等于 0） 则
            归 字节.从文字（「」）；
        终
        令 各块：列 为 【】；
        令 剩余：数 为 数量；
        当 （剩余 大于 0） 则
            若 （字节.长度（此.缓冲） 等于 0） 则
                定 读取量：数 为 数学.最小（8192，剩余）；
                若 非 此.接收更多（读取量） 则
                    报错（结束代码，「连接在完整正文到达前结束」）；
                终
            终
            定 取量：数 为 数学.最小（剩余，字节.长度（此.缓冲））；
            追加（各块，字节.切片（此.缓冲，0，取量））；
            置 此.缓冲 为 字节.切片（此.缓冲，取量，字节.长度（此.缓冲））；
            置 剩余 为 （剩余 减 取量）；
        终
        归 拼接字节列（各块）；
    终
    私 法 读取行为（最大行：数，超限代码：文，结束代码：文）：典 则
        定 CRLF：字节串 为 字节.从文字（「\n」）；
        当 真 则
            定 终点：数? 为 字节.查找（此.缓冲，CRLF）；
            若 （终点 是 数） 则
                若 （终点 大于 最大行） 则
                    报错（超限代码，「协议行超过配置预算」）；
                终
                定 行字节：字节串 为 字节.切片（此.缓冲，0，终点）；
                置 此.缓冲 为 字节.切片（此.缓冲，（终点 加 2），字节.长度（此.缓冲））；
                逐 所值：数 于 字节.转数列（行字节） 则
                    若 （所值 大于 127） 则
                        报错（「CHUNK_LINE_ASCII」，「分块与尾部行只接受 ASCII」）；
                    终
                终
                归 {「文字」：字节.转文字（行字节），「字节数」：终点}；
            终
            若 （字节.长度（此.缓冲） 大于 最大行） 则
                报错（超限代码，「协议行超过配置预算」）；
            终
            定 尚可读：数 为 （（最大行 加 2） 减 字节.长度（此.缓冲））；
            若 （尚可读 小于 1） 则
                报错（超限代码，「协议行缺少预算内的 CRLF」）；
            终
            若 非 此.接收更多（数学.最小（8192，尚可读）） 则
                报错（结束代码，「连接在协议行完成前结束」）；
            终
        终
        报错（「INTERNAL」，「协议行读取循环异常结束」）；
    终
    私 法 读取首部文字（）：文? 则
        定 分隔：字节串 为 字节.从文字（「\n\n」）；
        当 真 则
            定 分隔位：数? 为 字节.查找（此.缓冲，分隔）；
            若 （分隔位 是 数） 则
                若 （分隔位 大于 此.所限制.最大首部） 则
                    报错（「LIMIT_HEADER_BYTES」，「请求首部超过配置预算」）；
                终
                定 首部字节：字节串 为 字节.切片（此.缓冲，0，分隔位）；
                置 此.缓冲 为 字节.切片（此.缓冲，（分隔位 加 4），字节.长度（此.缓冲））；
                试 则
                    归 字节.转文字（首部字节）；
                救 所误 则
                    报错（「HEADER_ENCODING」，「请求首部不是有效 UTF-8」）；
                终
            终
            若 （字节.长度（此.缓冲） 大于 此.所限制.最大首部） 则
                报错（「LIMIT_HEADER_BYTES」，「请求首部超过配置预算」）；
            终
            定 尚可读：数 为 （（此.所限制.最大首部 加 4） 减 字节.长度（此.缓冲））；
            若 （尚可读 小于 1） 则
                报错（「LIMIT_HEADER_BYTES」，「请求首部缺少预算内的结束分隔」）；
            终
            若 非 此.接收更多（数学.最小（8192，尚可读）） 则
                若 （字节.长度（此.缓冲） 等于 0） 则
                    归 空；
                终
                报错（「EOF_HEADER」，「连接在请求首部完成前结束」）；
            终
        终
        报错（「INTERNAL」，「请求首部读取循环异常结束」）；
    终
    私 法 读取分块请求（头请求：HTTP请求）：HTTP请求 则
        令 各块：列 为 【】；
        令 总正文：数 为 0；
        令 分块数：数 为 0；
        当 真 则
            定 大小行：典 为 此.读取行为（此.所限制.最大分块行，「LIMIT_CHUNK_LINE」，「EOF_CHUNK_LINE」）；
            定 大小：数 为 分块大小（大小行【「文字」】，此.所限制.最大正文）；
            若 （大小 等于 0） 则
                令 各尾部行：列<文> 为 【】；
                令 尾部字节：数 为 0；
                当 真 则
                    定 尾部行：典 为 此.读取行为（此.所限制.最大尾部，「LIMIT_TRAILER_BYTES」，「EOF_TRAILER」）；
                    若 （尾部行【「文字」】 等于 「」） 则
                        定 尾部列：典 为 解析尾部行（各尾部行，此.所限制）；
                        定 结果：HTTP分块结果 为 HTTP分块结果（拼接字节列（各块），尾部列，0，此.缓冲）；
                        归 附分块正文（头请求，结果）；
                    终
                    置 尾部字节 为 （（尾部字节 加 尾部行【「字节数」】） 加 2）；
                    若 （尾部字节 大于 此.所限制.最大尾部） 则
                        报错（「LIMIT_TRAILER_BYTES」，「尾部字段超过配置预算」）；
                    终
                    追加（各尾部行，尾部行【「文字」】）；
                终
            终
            置 分块数 为 （分块数 加 1）；
            若 （分块数 大于 此.所限制.最大分块项） 则
                报错（「LIMIT_CHUNK_COUNT」，「分块数量超过配置预算」）；
            终
            置 总正文 为 （总正文 加 大小）；
            若 （总正文 大于 此.所限制.最大正文） 则
                报错（「LIMIT_BODY_BYTES」，「分块正文总量超过配置预算」）；
            终
            追加（各块，此.读取字节数（大小，「EOF_CHUNK_DATA」））；
            定 行尾：字节串 为 此.读取字节数（2，「EOF_CHUNK_DATA」）；
            若 （行尾 不等于 字节.从文字（「\n」）） 则
                报错（「CHUNK_DATA_END」，「分块数据后必须紧跟 CRLF」）；
            终
        终
        报错（「INTERNAL」，「会话分块读取循环异常结束」）；
    终
    私 法 完成请求（请求：HTTP请求）：HTTP请求 则
        置 此.请求数值 为 （此.请求数值 加 1）；
        置 此.可复用值 为 请求.保持连接（）；
        若 （此.请求数值 不小于 此.所限制.最大连接请求） 则
            置 此.可复用值 为 假；
        终
        若 非 此.可复用值 则
            请求.标记必须关闭（）；
        终
        归 请求；
    终
    私 法 读取下一请求内（）：HTTP请求? 则
        此.确保开放（）；
        若 （（此.请求数值 大于 0） 且 非 此.可复用值） 则
            归 空；
        终
        定 首部原文：文? 为 此.读取首部文字（）；
        若 （首部原文 等于 空） 则
            归 空；
        终
        若 （首部原文 是 文） 则
            定 头请求：HTTP请求 为 构造请求头为（首部原文，此.所限制）；
            若 （头请求.正文模式（） 等于 「分块」） 则
                归 此.完成请求（此.读取分块请求（头请求））；
            终
            定 正文：字节串 为 此.读取字节数（头请求.内容长度，「EOF_BODY」）；
            归 此.完成请求（附正文（头请求，正文））；
        终
        报错（「INTERNAL」，「请求首部可空状态不一致」）；
    终
    公 法 读取下一请求（）：HTTP请求? 则
        试 则
            归 此.读取下一请求内（）；
        救 所误 则
            置 此.可复用值 为 假；
            抛 所误；
        终
        报错（「INTERNAL」，「请求读取错误边界异常结束」）；
    终
    公 法 读取请求（）：HTTP请求 则
        定 请求：HTTP请求? 为 此.读取下一请求（）；
        若 （请求 是 HTTP请求） 则
            归 请求；
        终
        报错（「EOF_CLEAN」，「连接没有下一项 HTTP 请求」）；
    终
    公 法 发送临时响应为（请求：HTTP请求，响应：HTTP响应，超时毫秒：数）：数 则
        此.确保开放（）；
        若 非 是整数范围（超时毫秒，1，最大超时毫秒） 则
            报错（「LIMIT_TIMEOUT」，「响应发送超时须为一至 86400000 毫秒的整数」）；
        终
        若 （（响应.状态码 小于 100） 或 （响应.状态码 不小于 200）） 则
            报错（「RESPONSE_INTERIM_STATUS」，「临时响应状态码须为 100 至 199」）；
        终
        若 （响应.状态码 等于 状态切换协议） 则
            报错（「UPGRADE_REQUIRED」，「101 响应须通过协议升级边界发送」）；
        终
        若 （此.响应数值 不小于 此.请求数值） 则
            报错（「RESPONSE_ORDER」，「没有待响应的请求」）；
        终
        试 则
            归 套接字库.发送字节（此.所连接，响应.编码为（请求.方法，真），超时毫秒）；
        救 所误 则
            置 此.可复用值 为 假；
            抛 所误；
        终
        报错（「INTERNAL」，「临时响应发送边界异常结束」）；
    终
    公 法 发送临时响应（请求：HTTP请求，响应：HTTP响应）：数 则
        归 此.发送临时响应为（请求，响应，此.所限制.超时毫秒）；
    终
    公 法 发送响应为（请求：HTTP请求，响应：HTTP响应，超时毫秒：数）：数 则
        此.确保开放（）；
        若 非 是整数范围（超时毫秒，1，最大超时毫秒） 则
            报错（「LIMIT_TIMEOUT」，「响应发送超时须为一至 86400000 毫秒的整数」）；
        终
        若 （响应.状态码 小于 200） 则
            报错（「RESPONSE_FINAL_STATUS」，「最终响应状态码不得小于 200」）；
        终
        若 （此.响应数值 不小于 此.请求数值） 则
            报错（「RESPONSE_ORDER」，「响应数量不得超过已读取请求数」）；
        终
        定 有效保持：理 为 （（请求.保持连接（） 且 非 请求.必须关闭（）） 且 非 响应.应关闭连接（））；
        令 已发送：数 为 0；
        试 则
            置 已发送 为 套接字库.发送字节（此.所连接，响应.编码为（请求.方法，有效保持），超时毫秒）；
        救 所误 则
            置 此.可复用值 为 假；
            抛 所误；
        终
        置 此.响应数值 为 （此.响应数值 加 1）；
        若 非 有效保持 则
            置 此.可复用值 为 假；
            此.关闭（）；
        终
        归 已发送；
    终
    公 法 发送响应（请求：HTTP请求，响应：HTTP响应）：数 则
        归 此.发送响应为（请求，响应，此.所限制.超时毫秒）；
    终
    公 法 升级WebSocket为（请求：HTTP请求，子协议：文?，超时毫秒：数）：HTTP升级通道 则
        此.确保开放（）；
        若 非 是整数范围（超时毫秒，1，最大超时毫秒） 则
            报错（「LIMIT_TIMEOUT」，「升级响应发送超时须为一至 86400000 毫秒的整数」）；
        终
        若 （此.请求数值 不等于 （此.响应数值 加 1）） 则
            报错（「UPGRADE_ORDER」，「升级时必须且只能有一个待响应请求」）；
        终
        定 握手：HTTPWebSocket握手 为 解析WebSocket握手（请求）；
        令 响应：HTTPWebSocket升级响应 为 握手.响应（）；
        若 （子协议 是 文） 则
            置 响应 为 握手.响应协议（子协议）；
        终
        试 则
            套接字库.发送字节（此.所连接，响应.编码（），超时毫秒）；
        救 所误 则
            置 此.可复用值 为 假；
            抛 所误；
        终
        定 通道：HTTP升级通道 为 HTTP升级通道（此.所连接，此.缓冲）；
        置 此.缓冲 为 字节.从文字（「」）；
        置 此.响应数值 为 （此.响应数值 加 1）；
        置 此.可复用值 为 假；
        置 此.已升级值 为 真；
        归 通道；
    终
    公 法 升级WebSocket（请求：HTTP请求）：HTTP升级通道 则
        归 此.升级WebSocket为（请求，空，此.所限制.超时毫秒）；
    终
    公 法 升级WebSocket协议（请求：HTTP请求，子协议：文）：HTTP升级通道 则
        归 此.升级WebSocket为（请求，子协议，此.所限制.超时毫秒）；
    终
    公 法 请求数（）：数 则
        归 此.请求数值；
    终
    公 法 响应数（）：数 则
        归 此.响应数值；
    终
    公 法 缓冲字节数（）：数 则
        归 字节.长度（此.缓冲）；
    终
    公 法 可复用（）：理 则
        归 （（（此.可复用值 且 非 此.已结束值） 且 非 此.已关闭值） 且 非 此.已升级值）；
    终
    公 法 已结束（）：理 则
        归 此.已结束值；
    终
    公 法 已升级（）：理 则
        归 此.已升级值；
    终
    公 法 关闭（）：空 则
        若 此.已升级值 则
            报错（「CONNECTION_UPGRADED」，「升级后须由 HTTP升级通道 关闭连接」）；
        终
        若 非 此.已关闭值 则
            套接字库.关闭（此.所连接）；
            置 此.已关闭值 为 真；
            置 此.可复用值 为 假；
        终
        归 空；
    终
终

公 法 连接会话为（所连接：套接字，所限制：HTTP限制）：HTTP连接 则
    归 HTTP连接（所连接，所限制）；
终

公 法 连接会话（所连接：套接字）：HTTP连接 则
    归 HTTP连接（所连接，限制（））；
终

公 法 读取请求为（连接：套接字，超时毫秒：数，最大首部字节：数，最大正文字节：数）：HTTP请求 则
    若 非 是整数范围（最大首部字节，1024，默认最大首部字节） 则
        报错（「LIMIT_HEADER_BYTES」，「首部预算须为 1024 至 65536 字节的整数」）；
    终
    定 所限制：HTTP限制 为 限制（）；
    定 请求行预算：数 为 数学.最小（默认最大请求行字节，最大首部字节）；
    所限制.设读取（超时毫秒，请求行预算，最大首部字节，最大正文字节）.设会话（1）；
    定 请求：HTTP请求 为 连接会话为（连接，所限制）.读取请求（）；
    请求.标记必须关闭（）；
    归 请求；
终

公 法 读取请求（连接：套接字）：HTTP请求 则
    归 读取请求为（连接，默认超时毫秒，默认最大首部字节，默认最大正文字节）；
终

公 法 发送响应为（连接：套接字，响应：HTTP响应，超时毫秒：数）：数 则
    归 套接字库.发送字节（连接，响应.编码（），超时毫秒）；
终

公 法 发送响应（连接：套接字，响应：HTTP响应）：数 则
    归 发送响应为（连接，响应，默认超时毫秒）；
终
