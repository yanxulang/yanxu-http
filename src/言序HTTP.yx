/// yanxu-http 0.1：严格、受预算约束的 HTTP/1.1 基础实现。
引「标准:文字」为 文字；
引「标准:正则」为 正则；
引「标准:编码」为 编码；
引「标准:字节」为 字节；
引「标准:套接字」为 套接字库；
引「标准:JSON」为 JSON；
引「标准:数学」为 数学；

公 定 版本：文 为「0.1.0」；
公 定 默认超时毫秒：数 为 5000；
公 定 默认最大首部字节：数 为 65536；
公 定 默认最大正文字节：数 为 4194304；

公 定 状态成功：数 为 200；
公 定 状态已创建：数 为 201；
公 定 状态无内容：数 为 204；
公 定 状态重定向：数 为 302；
公 定 状态错误请求：数 为 400；
公 定 状态未找到：数 为 404；
公 定 状态方法不允许：数 为 405；
公 定 状态正文过大：数 为 413；
公 定 状态服务器错误：数 为 500；

法 数码值（字：文）：数 则
    若 字 等于「0」则 归 0；终
    若 字 等于「1」则 归 1；终
    若 字 等于「2」则 归 2；终
    若 字 等于「3」则 归 3；终
    若 字 等于「4」则 归 4；终
    若 字 等于「5」则 归 5；终
    若 字 等于「6」则 归 6；终
    若 字 等于「7」则 归 7；终
    若 字 等于「8」则 归 8；终
    若 字 等于「9」则 归 9；终
    归 减 1；
终

法 十进制整数（原文：文）：数 则
    若 原文 等于「」则 抛「HTTP_NUMBER：十进制整数不可为空」；终
    令 结果：数 为 0；
    逐 字：文 于 文字.字符列（原文）则
        定 数码：数 为 数码值（字）；
        若 数码 小于 0 则 抛「HTTP_NUMBER：只允许十进制数码」；终
        置 结果 为 结果 乘 10 加 数码；
        若 结果 大于 4194304 则 抛「HTTP_LIMIT：数值超过 4 MiB 预算」；终
    终
    归 结果；
终

法 数码文（值：数）：文 则
    若 值 等于 0 则 归「0」；终
    若 值 等于 1 则 归「1」；终
    若 值 等于 2 则 归「2」；终
    若 值 等于 3 则 归「3」；终
    若 值 等于 4 则 归「4」；终
    若 值 等于 5 则 归「5」；终
    若 值 等于 6 则 归「6」；终
    若 值 等于 7 则 归「7」；终
    若 值 等于 8 则 归「8」；终
    若 值 等于 9 则 归「9」；终
    抛「HTTP_NUMBER：数码越界」；
终

法 整数文（值：数）：文 则
    若 （值 小于 0）或（数学.下取整（值）不等于 值）则 抛「HTTP_NUMBER：需要非负整数」；终
    若 值 等于 0 则 归「0」；终
    令 余值：数 为 值；
    令 结果：文 为「」；
    当 余值 大于 0 则
        定 商：数 为 数学.下取整（余值 除 10）；
        置 结果 为 数码文（余值 减 商 乘 10）加 结果；
        置 余值 为 商；
    终
    归 结果；
终

法 首部名合法（名称：文）：理 则
    归 正则.匹配（「^[!#$%&'*+.^_`|~0-9A-Za-z-]+$」，名称）；
终

法 含换行或零字节（值：文）：理 则
    逐 所字节：数 于 字节.转数列（字节.从文字（值））则
        若 （所字节 等于 0）或（所字节 等于 10）或（所字节 等于 13）则 归 真；终
    终
    归 假；
终

法 断言首部（名称：文，值：文）：空 则
    若 非 首部名合法（名称）则 抛「HTTP_HEADER：首部名不合法」；终
    若 含换行或零字节（值）则 抛「HTTP_HEADER：首部值不得含换行或零字节」；终
    归 空；
终

法 余项联结（各项：列，分隔：文）：文 则
    若 长度（各项）小于 2 则 归「」；终
    令 结果：列<文> 为【】；
    令 次：数 为 1；
    当 次 小于 长度（各项）则
        追加（结果，各项【次】）；
        置 次 为 次 加 1；
    终
    归 文字.联结（结果，分隔）；
终

法 百分号解码（原文：文）：文 则
    归 编码.解百分号（文字.替换（原文，「+」，「 」））；
终

法 解析目标（目标：文）：典 则
    若 目标 等于「*」则 归 {「路径」：「*」，「查询」：{}}；终
    若 非 文字.始于（目标，「/」）则 抛「HTTP_TARGET：0.1 只接受 origin-form 请求目标」；终
    若 包含（目标，「#」）则 抛「HTTP_TARGET：请求目标不得含片段」；终
    定 各部：列<文> 为 文字.分割（目标，「?」）；
    定 路径：文 为 各部【0】；
    令 查询原文：文 为「」；
    若 长度（各部）大于 1 则 置 查询原文 为 余项联结（各部，「?」）；终
    令 查询：典 为 {}；
    若 查询原文 不等于「」则
        逐 所对：文 于 文字.分割（查询原文，「&」）则
            若 所对 不等于「」则
                定 键值：列<文> 为 文字.分割（所对，「=」）；
                定 键：文 为 百分号解码（键值【0】）；
                定 值：文 为 百分号解码（余项联结（键值，「=」））；
                若 非 有键（查询，键）则 置 查询【键】为【】；终
                追加（查询【键】，值）；
            终
        终
    终
    归 {「路径」：百分号解码（路径），「查询」：查询}；
终

公 法 解析Cookie（原文：文）：典 则
    令 结果：典 为 {}；
    逐 所项：文 于 文字.分割（原文，「;」）则
        定 项：文 为 文字.修剪（所项）；
        若 项 不等于「」则
            定 键值：列<文> 为 文字.分割（项，「=」）；
            定 键：文 为 文字.修剪（键值【0】）；
            若 键 不等于「」则 置 结果【键】为 余项联结（键值，「=」）；终
        终
    终
    归 结果；
终

/// 已解析的 HTTP 请求。
公 类 HTTP请求 则
    公 只 域 方法：文；
    公 只 域 目标：文；
    公 只 域 路径：文；
    公 只 域 版本：文；
    公 只 域 首部：典；
    公 只 域 查询：典；
    公 只 域 Cookie：典；
    公 只 域 正文：字节串；
    公 只 域 内容长度：数；

    公 法 初始化（方法：文，目标：文，路径：文，版本：文，首部：典，查询：典，Cookie：典，正文：字节串，内容长度：数）则
        置 此.方法 为 方法；
        置 此.目标 为 目标；
        置 此.路径 为 路径；
        置 此.版本 为 版本；
        置 此.首部 为 首部；
        置 此.查询 为 查询；
        置 此.Cookie 为 Cookie；
        置 此.正文 为 正文；
        置 此.内容长度 为 内容长度；
    终

    公 法 首部值（名称：文）：文? 则
        定 小名：文 为 文字.小写（名称）；
        若 有键（此.首部，小名）则 归 此.首部【小名】；终
        归 空；
    终

    公 法 查询值（名称：文）：文? 则
        若 非 有键（此.查询，名称）则 归 空；终
        定 各值：列 为 此.查询【名称】；
        若 长度（各值）等于 0 则 归 空；终
        归 各值【0】；
    终

    公 法 查询全部（名称：文）：列 则
        若 有键（此.查询，名称）则 归 此.查询【名称】；终
        归【】；
    终

    公 法 Cookie值（名称：文）：文? 则
        若 有键（此.Cookie，名称）则 归 此.Cookie【名称】；终
        归 空；
    终

    公 法 正文文字（）：文 则
        归 字节.转文字（此.正文）；
    终
终

法 构造请求头（原文：文）：HTTP请求 则
    定 各行：列<文> 为 文字.分割（原文，「\r\n」）；
    若 文字.联结（各行，「\r\n」）不等于 原文 则 抛「HTTP_LINE：请求首部必须使用 CRLF」；终
    若 长度（各行）小于 2 则 抛「HTTP_REQUEST_LINE：缺少请求首部」；终
    定 请求项：列<文> 为 正则.分割（「[ ]+」，文字.修剪（各行【0】））；
    若 长度（请求项）不等于 3 则 抛「HTTP_REQUEST_LINE：请求行必须含方法、目标和版本」；终
    定 方法：文 为 请求项【0】；
    定 目标：文 为 请求项【1】；
    定 所版本：文 为 请求项【2】；
    若 非 首部名合法（方法）则 抛「HTTP_METHOD：方法不是合法 token」；终
    若 所版本 不等于「HTTP/1.1」则 抛「HTTP_VERSION：0.1 只支持 HTTP/1.1」；终

    令 首部：典 为 {}；
    令 次：数 为 1；
    当 次 小于 长度（各行）则
        定 行：文 为 各行【次】；
        若 行 等于「」则 抛「HTTP_HEADER：首部区内不得有空行」；终
        若 （文字.始于（行，「 」））或（文字.始于（行，「\t」））则 抛「HTTP_HEADER：不支持折叠首部」；终
        定 键值：列<文> 为 文字.分割（行，「:」）；
        若 长度（键值）小于 2 则 抛「HTTP_HEADER：首部缺少冒号」；终
        定 原名：文 为 键值【0】；
        定 值：文 为 文字.修剪（余项联结（键值，「:」））；
        断言首部（原名，值）；
        定 小名：文 为 文字.小写（原名）；
        若 有键（首部，小名）则
            若 （小名 等于「host」）或（小名 等于「content-length」）则 抛「HTTP_HEADER：关键首部不得重复」；终
            若 小名 等于「cookie」则
                置 首部【小名】为 首部【小名】加「; 」加 值；
            否则
                置 首部【小名】为 首部【小名】加「, 」加 值；
            终
        否则
            置 首部【小名】为 值；
        终
        置 次 为 次 加 1；
    终
    若 （非 有键（首部，「host」））或（首部【「host」】等于「」）则 抛「HTTP_HOST：HTTP/1.1 请求必须含 Host」；终
    若 有键（首部，「transfer-encoding」）则 抛「HTTP_TRANSFER_ENCODING：0.1 尚不支持请求分块传输」；终
    令 内容长度：数 为 0；
    若 有键（首部，「content-length」）则 置 内容长度 为 十进制整数（首部【「content-length」】）；终
    定 目标信息：典 为 解析目标（目标）；
    令 各Cookie：典 为 {}；
    若 有键（首部，「cookie」）则 置 各Cookie 为 解析Cookie（首部【「cookie」】）；终
    归 HTTP请求（方法，目标，目标信息【「路径」】，所版本，首部，目标信息【「查询」】，各Cookie，字节.从文字（「」），内容长度）；
终

法 附正文（请求：HTTP请求，正文：字节串）：HTTP请求 则
    若 字节.长度（正文）不等于 请求.内容长度 则 抛「HTTP_BODY_LENGTH：正文长度与 Content-Length 不一致」；终
    归 HTTP请求（请求.方法，请求.目标，请求.路径，请求.版本，请求.首部，请求.查询，请求.Cookie，正文，请求.内容长度）；
终

/// 解析不含末尾 CRLFCRLF 的请求首部。
公 法 解析请求头（原文：文）：HTTP请求 则
    归 构造请求头（原文）；
终

/// 解析请求首部与已经按 Content-Length 取得的正文。
公 法 解析请求（首部原文：文，正文：字节串）：HTTP请求 则
    归 附正文（构造请求头（首部原文），正文）；
终

法 状态短语（状态码：数）：文 则
    若 状态码 等于 100 则 归「Continue」；终
    若 状态码 等于 200 则 归「OK」；终
    若 状态码 等于 201 则 归「Created」；终
    若 状态码 等于 204 则 归「No Content」；终
    若 状态码 等于 301 则 归「Moved Permanently」；终
    若 状态码 等于 302 则 归「Found」；终
    若 状态码 等于 304 则 归「Not Modified」；终
    若 状态码 等于 400 则 归「Bad Request」；终
    若 状态码 等于 401 则 归「Unauthorized」；终
    若 状态码 等于 403 则 归「Forbidden」；终
    若 状态码 等于 404 则 归「Not Found」；终
    若 状态码 等于 405 则 归「Method Not Allowed」；终
    若 状态码 等于 409 则 归「Conflict」；终
    若 状态码 等于 413 则 归「Content Too Large」；终
    若 状态码 等于 415 则 归「Unsupported Media Type」；终
    若 状态码 等于 422 则 归「Unprocessable Content」；终
    若 状态码 等于 429 则 归「Too Many Requests」；终
    若 状态码 等于 500 则 归「Internal Server Error」；终
    若 状态码 等于 501 则 归「Not Implemented」；终
    若 状态码 等于 503 则 归「Service Unavailable」；终
    归「Unknown」；
终

法 断言Cookie名（名称：文）：空 则
    若 非 首部名合法（名称）则 抛「HTTP_COOKIE：Cookie 名不合法」；终
    归 空；
终

法 断言Cookie值（值：文）：空 则
    逐 所字节：数 于 字节.转数列（字节.从文字（值））则
        若 （所字节 小于 33）或（所字节 大于 126）或（所字节 等于 34）或（所字节 等于 44）或（所字节 等于 59）或（所字节 等于 92）则
            抛「HTTP_COOKIE：Cookie 值须为安全 ASCII cookie-octet」；
        终
    终
    归 空；
终

/// 响应 Cookie；默认 Path=/、HttpOnly、SameSite=Lax。
公 类 HTTPCookie 则
    公 只 域 名称：文；
    公 只 域 值：文；
    公 域 路径：文 为「/」；
    公 域 域名：文? 为 空；
    公 域 最大秒：数? 为 空；
    公 域 安全：理 为 假；
    公 域 仅HTTP：理 为 真；
    公 域 同站：文 为「Lax」；

    公 法 初始化（名称：文，值：文）则
        断言Cookie名（名称）；
        断言Cookie值（值）；
        置 此.名称 为 名称；
        置 此.值 为 值；
    终

    公 法 设路径（路径：文）：HTTPCookie 则
        若 含换行或零字节（路径）或 包含（路径，「;」）则 抛「HTTP_COOKIE：Cookie Path 不合法」；终
        置 此.路径 为 路径；
        归 此；
    终

    公 法 设域名（域名：文）：HTTPCookie 则
        若 含换行或零字节（域名）或 包含（域名，「;」）则 抛「HTTP_COOKIE：Cookie Domain 不合法」；终
        置 此.域名 为 域名；
        归 此；
    终

    公 法 设最大秒（秒：数）：HTTPCookie 则
        若 数学.下取整（秒）不等于 秒 则 抛「HTTP_COOKIE：Max-Age 必须是整数」；终
        置 此.最大秒 为 秒；
        归 此；
    终

    公 法 设安全（启用：理）：HTTPCookie 则 置 此.安全 为 启用；归 此；终
    公 法 设仅HTTP（启用：理）：HTTPCookie 则 置 此.仅HTTP 为 启用；归 此；终

    公 法 设同站（策略：文）：HTTPCookie 则
        若 （策略 不等于「Strict」）且（策略 不等于「Lax」）且（策略 不等于「None」）则 抛「HTTP_COOKIE：SameSite 只接受 Strict、Lax 或 None」；终
        置 此.同站 为 策略；
        归 此；
    终

    公 法 渲染（）：文 则
        断言Cookie名（此.名称）；
        断言Cookie值（此.值）；
        若 （此.同站 等于「None」）且（非 此.安全）则 抛「HTTP_COOKIE：SameSite=None 必须同时启用 Secure」；终
        令 各项：列<文> 为【此.名称 加「=」加 此.值】；
        若 此.路径 不等于「」则 追加（各项，「Path=」加 此.路径）；终
        定 所域名：文? 为 此.域名；
        若 所域名 是 文 则 追加（各项，「Domain=」加 所域名）；终
        定 所最大秒：数? 为 此.最大秒；
        若 所最大秒 是 数 则
            若 所最大秒 小于 0 则
                追加（各项，「Max-Age=-」加 整数文（减 所最大秒））；
            否则
                追加（各项，「Max-Age=」加 整数文（所最大秒））；
            终
        终
        若 此.安全 则 追加（各项，「Secure」）；终
        若 此.仅HTTP 则 追加（各项，「HttpOnly」）；终
        追加（各项，「SameSite=」加 此.同站）；
        归 文字.联结（各项，「; 」）；
    终
终

/// HTTP 响应；编码时固定补充 Content-Length 与 Connection: close。
公 类 HTTP响应 则
    公 只 域 状态码：数；
    私 域 首部：典；
    私 域 Cookie列：列；
    公 只 域 正文：字节串；

    公 法 初始化（状态码：数，正文：字节串）则
        若 （状态码 小于 100）或（状态码 大于 599）或（数学.下取整（状态码）不等于 状态码）则 抛「HTTP_STATUS：状态码须为 100 至 599 的整数」；终
        置 此.状态码 为 状态码；
        置 此.首部 为 {}；
        置 此.Cookie列 为【】；
        置 此.正文 为 正文；
    终

    公 法 设首部（名称：文，值：文）：HTTP响应 则
        断言首部（名称，值）；
        定 小名：文 为 文字.小写（名称）；
        若 （小名 等于「content-length」）或（小名 等于「connection」）或（小名 等于「set-cookie」）则 抛「HTTP_HEADER：该首部由响应编码器管理」；终
        置 此.首部【小名】为 值；
        归 此；
    终

    公 法 首部值（名称：文）：文? 则
        定 小名：文 为 文字.小写（名称）；
        若 有键（此.首部，小名）则 归 此.首部【小名】；终
        归 空；
    终

    公 法 添Cookie（所Cookie：HTTPCookie）：HTTP响应 则
        追加（此.Cookie列，所Cookie）；
        归 此；
    终

    公 法 编码（）：字节串 则
        令 各行：列<文> 为【「HTTP/1.1 」加 整数文（此.状态码）加「 」加 状态短语（此.状态码）】；
        逐 名称 于 排序（键列（此.首部））则
            断言首部（名称，此.首部【名称】）；
            追加（各行，名称 加「: 」加 此.首部【名称】）；
        终
        逐 所Cookie：HTTPCookie 于 此.Cookie列 则
            追加（各行，「set-cookie: 」加 所Cookie.渲染（））；
        终
        追加（各行，「content-length: 」加 整数文（字节.长度（此.正文）））；
        追加（各行，「connection: close」）；
        定 首部文：文 为 文字.联结（各行，「\r\n」）加「\r\n\r\n」；
        归 字节.拼接（字节.从文字（首部文），此.正文）；
    终
终

公 法 Cookie（名称：文，值：文）：HTTPCookie 则
    归 HTTPCookie（名称，值）；
终

公 法 响应字节（状态码：数，正文：字节串，内容类型：文）：HTTP响应 则
    定 响应：HTTP响应 为 HTTP响应（状态码，正文）；
    响应.设首部（「content-type」，内容类型）；
    归 响应；
终

公 法 字节响应（正文：字节串）：HTTP响应 则
    归 响应字节（200，正文，「application/octet-stream」）；
终

公 法 文字响应（正文：文）：HTTP响应 则
    归 响应字节（200，字节.从文字（正文），「text/plain; charset=utf-8」）；
终

公 法 HTML响应（正文：文）：HTTP响应 则
    归 响应字节（200，字节.从文字（正文），「text/html; charset=utf-8」）；
终

公 法 JSON响应（值）：HTTP响应 则
    归 响应字节（200，字节.从文字（JSON.序列化（值）），「application/json; charset=utf-8」）；
终

/// 正文须由言据库序列化；HTTP 层只负责媒体类型与传输。
公 法 言据响应（正文：文）：HTTP响应 则
    归 响应字节（200，字节.从文字（正文），「application/vnd.yanxu.yanju; charset=utf-8」）；
终

公 法 状态响应（状态码：数，正文：文）：HTTP响应 则
    归 响应字节（状态码，字节.从文字（正文），「text/plain; charset=utf-8」）；
终

公 法 重定向响应（地址：文，状态码：数）：HTTP响应 则
    若 （状态码 不等于 301）且（状态码 不等于 302）且（状态码 不等于 303）且（状态码 不等于 307）且（状态码 不等于 308）则 抛「HTTP_STATUS：重定向状态码不合法」；终
    定 响应：HTTP响应 为 HTTP响应（状态码，字节.从文字（「」））；
    响应.设首部（「location」，地址）；
    归 响应；
终

/// 从 TCP 流读取一个请求；每个连接只处理这一项请求。
公 法 读取请求为（连接：套接字，超时毫秒：数，最大首部字节：数，最大正文字节：数）：HTTP请求 则
    若 （最大首部字节 小于 1024）或（最大首部字节 大于 65536）则 抛「HTTP_LIMIT：首部预算须在 1024 至 65536 字节」；终
    若 （最大正文字节 小于 0）或（最大正文字节 大于 4194304）则 抛「HTTP_LIMIT：正文预算须在 0 至 4 MiB」；终
    定 分隔：字节串 为 字节.从文字（「\r\n\r\n」）；
    令 缓冲：字节串 为 字节.从文字（「」）；
    令 分隔位：数? 为 空；
    当 分隔位 等于 空 则
        定 所读：典 为 套接字库.接收字节（连接，8192，超时毫秒）；
        若 所读【「已结束」】则 抛「HTTP_EOF：请求首部未完成连接即结束」；终
        置 缓冲 为 字节.拼接（缓冲，所读【「数据」】）；
        置 分隔位 为 字节.查找（缓冲，分隔）；
        若 （分隔位 等于 空）且（字节.长度（缓冲）大于 最大首部字节）则 抛「HTTP_LIMIT：请求首部超过预算」；终
    终
    若 分隔位 是 数 则
        定 首部终点：数 为 分隔位；
        若 首部终点 大于 最大首部字节 则 抛「HTTP_LIMIT：请求首部超过预算」；终
        定 首部原文：文 为 字节.转文字（字节.切片（缓冲，0，首部终点））；
        定 头请求：HTTP请求 为 构造请求头（首部原文）；
        若 头请求.内容长度 大于 最大正文字节 则 抛「HTTP_LIMIT：请求正文超过预算」；终
        定 已有起点：数 为 首部终点 加 4；
        令 已有正文：字节串 为 字节.切片（缓冲，已有起点，字节.长度（缓冲））；
        若 字节.长度（已有正文）大于 头请求.内容长度 则
            置 已有正文 为 字节.切片（已有正文，0，头请求.内容长度）；
        终
        令 正文：字节串 为 已有正文；
        定 所缺：数 为 头请求.内容长度 减 字节.长度（已有正文）；
        若 所缺 大于 0 则
            置 正文 为 字节.拼接（已有正文，套接字库.精确读取（连接，所缺，超时毫秒））；
        终
        归 附正文（头请求，正文）；
    终
    抛「HTTP_INTERNAL：请求分隔状态不一致」；
终

公 法 读取请求（连接：套接字）：HTTP请求 则
    归 读取请求为（连接，默认超时毫秒，默认最大首部字节，默认最大正文字节）；
终

公 法 发送响应为（连接：套接字，响应：HTTP响应，超时毫秒：数）：数 则
    归 套接字库.发送字节（连接，响应.编码（），超时毫秒）；
终

公 法 发送响应（连接：套接字，响应：HTTP响应）：数 则
    归 发送响应为（连接，响应，默认超时毫秒）；
终
